---
type: principle
domain: [desktop, ui, startup]
confidence: 0.95
sources: 1
entities: [tools/desktop/app.go, configInjectingHandler, apiBaseWaitTimeout, bootBadgeColor]
refs: ['src://7b4887ce51d9/tools/desktop/app.go@d13e91d18ffe69b4b255cb92319a6299885847f4:2d4ea17c1e488a3f5dbe678cadf19f6de95265b2', 'src://7b4887ce51d9/tools/desktop/serverboot.go@d13e91d18ffe69b4b255cb92319a6299885847f4:edf5d0c4fb13602af43bf41facb0f7c3472a944d', 'src://7b4887ce51d9/web/src/bootStatus.ts@d13e91d18ffe69b4b255cb92319a6299885847f4:b0cc7f8d99d26aaf61d8b505f50fecf0d1ec65bb', 'src://7b4887ce51d9/web/src/boot.ts@d13e91d18ffe69b4b255cb92319a6299885847f4:4acebfedc25d837a207f67963f31022c8626dfb2', 'kb://3ec012f5b4d2/kb/invariants/desktop/startup/wails/fabd21af.md']
---
# The desktop window is Hidden at creation, and /config.js answers IMMEDIATELY — flagging __KNOMIT_BOOTING__ rather than blocking — because a first launch spends minutes downloading models

Two coupled choices in tools/desktop/app.go. The first is unchanged; the second was REVERSED, and the old form is now a documented trap.

**The window is Hidden at creation** (`Hidden: true` in the WebviewWindowOptions). Knomit is a tray app: the menu bar is the entry point and the window opens only when asked, including on the login-item launch.

**/config.js no longer blocks on the booting server.** It used to: `configInjectingHandler` called `serverBoot.wait` with a 90s `apiBaseWaitTimeout`, on the reasoning that the webview should take a moment longer to paint rather than load against an address that does not exist. That is a good trade for a two-second boot and a bad one for a FIRST launch, which spends minutes fetching ~640 MB of model artifacts (`EnsureModel`, called from `app.New` BEFORE `bootServer` ever listens).

Why blocking was worse than slow: web/index.html loads config.js with a plain `<script src="./config.js">` tag, so holding that request holds the whole DOCUMENT — the user watched a static splash with no way to learn what was happening. And the wait was CAPPED: past 90s config.js 503'd, the page loaded with no `__KNOMIT_API_BASE__`, and `apiBase()` then resolved every request against the webview origin, where the handler's own SPA fallback answers **200 with index.html**. HTML-where-JSON-was-expected is indistinguishable from a real failure, so the UI retried forever and never recovered even after the server came up.

WHAT IT DOES NOW: answers immediately and says which world the client is in — `__KNOMIT_API_BASE__` when the server is up, `__KNOMIT_BOOTING__ = true` when it is not (and NO base, deliberately: the absence is what makes the client's gate unmissable). The client polls `GET /boot/status`, served by the same desktop handler, and adopts the base from it with no reload. `apiBaseWaitTimeout` is deleted.

CONSEQUENCE for anyone touching this handler: nothing serving an HTTP REQUEST may call `serverBoot.wait`. Use `serverBoot.status()`, which is non-blocking and whose non-blocking select is also what preserves the struct's no-mutex synchronisation (apiBase/err are read only on the branch where `done` is already closed). `wait` survives for run()'s tray-badge/fatal-dialog goroutine, which genuinely wants to sleep until the boot settles.

The phases are a WIRE FORMAT shared with web/src/boot.ts: starting, installing-tools, downloading-models, starting-engine, starting-server, ready, failed. An unknown value degrades to a generic "Starting…" there rather than breaking, so change both sides together.

WHAT THIS DOES NOT MEAN: the Hidden-window rule is untouched, and so is the invariant that bootKnomit runs on its own goroutine and never touches Wails. Serving /boot/status adds no listener and no main-thread work.
