---
type: observation
domain: [web, rest, motif, mn6]
confidence: 0.95
sources: 1
entities: [motifParams, internal/web/params.go, TestMotifMatch_LooseTiersAreNeverReachedByAutomation, looseTierRefusers, MotifMatchToken1, MotifMatchSoft]
motifs: [allowlist-avoids-naming-forbidden]
refs: ['src://7b4887ce51d9/internal/web/params.go@7a0d2c3116bdf25eeaa047bd5a35df0da27764c5:0b8a86e05f88f7cfd39d1bf2f78f605844697119', 'src://7b4887ce51d9/internal/mcp/motif_surfaces_test.go@7a0d2c3116bdf25eeaa047bd5a35df0da27764c5:8df8b27318fb257345fa188fbf2e2c51656b8714', 'https://github.com/knomit/knomit/pull/174']
---
# Web REST accepts motif tiers exact/stem/token-2 only, validated by an allowlist that never names the loose tiers — and the MN6 AST guard now scans internal/web

Options considered: (1) exact + stem + token-2, the same ladder MCP exposes for automation (chosen); (2) all tiers except soft, letting a human explore token-1 from the UI, requiring a looseTierRefusers exemption; (3) exact only.

Rationale: MN6 doctrine — loose tiers exist for a caller who TYPES them, never as anything automation selects; the web surface feeds programmatic re-issue (chips, links), so it stays on the automation ladder. The validation in motifParams is an ALLOWLIST switch over the accepted tier constants, so the identifiers MotifMatchToken1/MotifMatchSoft and the strings "token-1"/"soft" appear NOWHERE in internal/web non-test code — which is what let the guard extend without any looseTierRefusers entry. TestMotifMatch_LooseTiersAreNeverReachedByAutomation's dirs list gained "../web".

Verified by sabotage, not trust: temporarily defaulting motifParams to the loose tier made the guard fail naming ../web/params.go.motifParams, then reverted.

Consequence for future web code: rejecting a loose tier must stay phrased as an allowlist ("accepted: exact, stem, token-2") — writing the rejected tier's name into a validation case or an error string will fail the MN6 guard, and the fix is rewording, never a refuser exemption for web.
