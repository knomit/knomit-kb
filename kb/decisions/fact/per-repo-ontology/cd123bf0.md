---
type: observation
domain: [fact, ontology, repos]
confidence: 0.85
sources: 0
entities: [repoBuilder.loadOntology, RepoInstance.ontology, RepoInstance.Ontology, fact.DefaultOntology, domains/ontology.yaml, internal/repos/builder.go, internal/repos/manager.go]
refs: ['src://knomit/.claude/plans/2026-04-01-per-repo-ontology.md@0938d83']
---
# Each repo loads its own domains/ontology.yaml at open time; Manager has NO shared ontology

Each repo loads its own ontology from domains/ontology.yaml on its agent branch at open time via repoBuilder.loadOntology() (falls back to fact.DefaultOntology() if missing/unparseable). Stored on RepoInstance.ontology, exposed via ri.Ontology(). Manager.Ontology() and Manager.ontology field are REMOVED — there is no shared/global ontology. SetupMCP reads from ri.Ontology(), not s.Manager.Ontology(). Manager.Boot() is single-phase — just opens knomit.db first then globs *.db; no special two-phase ontology loading. The ontology is captured at repo OPEN time — if domains/ontology.yaml changes in git after that, the in-memory ontology is stale until the repo is reopened.
