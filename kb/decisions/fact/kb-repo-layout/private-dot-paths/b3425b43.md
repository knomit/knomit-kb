---
kind: pragmatic
type: policy
domain: [fact, store, okf, mcp, web, indexing]
confidence: 0.95
sources: 1
entities: [fact.IsPrivatePath, repoHandler.isFactPath, internal/fact/private.go, internal/store/factpath.go, internal/okf/source/facts.go, looksLikeFact, internal/web/handlers_fact_create.go, internal/web/handlers_fact_write.go, internal/mcp/learn.go, internal/mcp/update.go, validateOutputPath, store.validatePath, .domains, .github, kb/.drafts]
refs: ['kb://3ec012f5b4d2/kb/invariants/fact/private-paths/b6babb00.md', 'kb://3ec012f5b4d2/kb/invariants/store/indexing/ontology-root-scope/1b35d56b.md', 'src://7b4887ce51d9/internal/fact/private.go@542903478691569def2d397977201ea5f0ab5fbf:96594a5f7497e024e3a2531b5ed9de42bbab4c9c', 'src://7b4887ce51d9/internal/store/factpath.go@542903478691569def2d397977201ea5f0ab5fbf:fdd3415d6dd43bd6e4311ae504e9964939cfdb0a', 'src://7b4887ce51d9/internal/okf/source/facts.go@542903478691569def2d397977201ea5f0ab5fbf:bc46189391f678cad47cba0e96f619476ad91f00', 'src://7b4887ce51d9/internal/store/fact_write.go@542903478691569def2d397977201ea5f0ab5fbf:3f68b610abeb0897e656a03ac5ce68bd0d9bb80a', 'https://github.com/knomit/knomit/pull/68']
---
# Any path segment beginning with '.' is private and excluded from fact DISCOVERY — chosen over discovery-only and over folders-only, and deliberately NOT enforced in store.validatePath

A KB repo accumulates directories that are machinery rather than knowledge — `.github/`, and now `.domains/`. They should be excluded by a stated rule rather than by each walker's incidental prefix test.

**Options considered.** (a) Any dot-prefixed segment — folder OR filename — private, skipped by every walker, AND rejected at the fact-creation boundary. (b) Same skip, but writes unchecked. (c) Dot-FOLDERS only, discovery-only, which is the literal original request.

**Rationale.** (a) won on two counts. Covering filenames as well as directories removes an asymmetry nobody would remember (`kb/architecture/.wip.md` would otherwise index as a fact). Rejecting on write prevents the worst available outcome: (b) would return 201/success for a fact that never appears in search — silent knowledge loss, and the hardest class of bug to trace to its cause. (c) was the smallest diff but left the filename asymmetry permanently.

**The choice.** `fact.IsPrivatePath(path) bool` — true when any `/`-separated segment begins with `.`.

**Where the enforcement actually landed — read the invariant, not this paragraph.** This fact was written from the DESIGN, before implementation, and its original counts were wrong: it said four walkers and three creation sites. The merged implementation has SEVEN discovery guards and FIVE write refusals, and the two extra sites in each column were found only in the final whole-branch review. The authoritative, verified site list lives in `kb/invariants/fact/private-paths/b6babb00.md`. Two corrections worth carrying here because a designer will otherwise reproduce the same undercount:

- `PUT /api/v1/.../facts/{path...}` (`internal/web/handlers_fact_write.go`) is a fact-CREATION site with a fully caller-supplied path. It does not look like one — it reads as an update endpoint — and a plan that enumerates "creation sites" from the topic/category-derived writers will miss it.
- The three ListDir-backed topic/lens browsers walk the raw git tree rather than the fact index, so they do NOT inherit `store.isFactPath`'s guard and are separate discovery surfaces.

**Private means excluded from DISCOVERY, never from ACCESS.** knomit still reads specific known paths by name — that is precisely what lets it load `.domains/ontology.yaml`. The rule governs walking, never opening. A human may still place files under `kb/.drafts/` by hand and knomit respects them as a private stash; the rejection binds only knomit's own write path.

**The trap.** This check must NOT go into `store.validatePath`, which looks like the ideal chokepoint because every write site reaches it. It also guards the writes for `README.md` and `.domains/ontology.yaml` — putting the private rule there breaks the boot-time ontology refresh.

**In the OKF exporter, order matters:** skip private paths BEFORE the `looksLikeFact` check, or every hand-placed draft is reported as lost knowledge on every export — a permanent false alarm that trains readers to ignore a real one.

**Relation to the ontology-root invariant.** This is still a LOCATION test, so it extends `kb/invariants/store/indexing/ontology-root-scope/1b35d56b.md` rather than competing with it: membership is decided by where a file sits, never by whether its bytes happen to parse.
