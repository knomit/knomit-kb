---
type: observation
domain: [synthesize, methodology, mcp, design-philosophy]
confidence: 0.85
sources: 0
entities: [RelevantMethodology, MethodologyMatch, internal/mcp/hypothesize.go, internal/synthesize/review.go, internal/synthesize/prompt_review.go]
refs: ['src://knomit/.claude/plans/2026-05-01-methodology-in-the-loop-design.md@0938d83']
---
# Methodology injection into prompts — server NEVER auto-cites, LLM does

Methodology application was broken: 40 methodology facts on an active agent branch had ZERO incoming refs (Louvain clustering never surfaced methodology; nothing cited it). Fix: at three injection sites (knomit_hypothesize work-items, knomit_review reflect prompt, synthesize distill prompt) the server calls RelevantMethodology(branch, sourceBody, sourceDomains, sourceEntities, k) and INJECTS the top matches into the prompt context as 'Applicable methodology' sections containing full title+path+body. The server records NOTHING automatically — every authoring decision (what to cite, how to tag) stays with the LLM. This preserves the meaning of refs as 'evidence the agent actually used' and gives honest empirical feedback signals (organic cite rate, organic tag-coverage rate). If retrieval is empty, the section is OMITTED entirely from the prompt — no empty heading, no placeholder text. Risk if LLM ignores: prompt tuning is the next lever, not algorithm changes.
