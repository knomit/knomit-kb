---
type: observation
domain: [synthesize, mcp, discovery]
confidence: 0.9
sources: 0
entities: [ScopeFilter, buildScoredBridges, enumerateBridgeCandidates, reshapeCohesiveSubset, renderDiscoverPrompt, DiscoverWorkPayload, BridgeSeedSet]
refs: ['src://knomit/internal/synthesize/bridge.go@9f8780e5', 'src://knomit/internal/synthesize/bridge_reshape.go@9f8780e5', 'src://knomit/internal/synthesize/discovery.go@9f8780e5', kb/decisions/synthesize/bridge-reshape-cross-community-seeding/44be8e4d.md, kb/decisions/synthesize/bridge-quality-mechanism/6d2f9df4.md]
---
# Filtered-case bridges: full token-optional generator via greedy seed-and-grow over the scoped SIMILAR_TO graph

Decision (2026-06-27, via AskUserQuestion) for Phase 5 of the bridge-quality mechanism — how the filtered (scope-active) discovery case generates bridges.

**Options considered:** (a) FULL token-optional generator — when a domain/entity scope is active, thread the ScopeFilter into the builder and enumerate cross-community cohesive sets DIRECTLY from the SIMILAR_TO graph over the scoped pool (greedy seed-and-grow reusing the Phase-4 reshape machinery: seed the densest cross-community SIMILAR_TO pair, grow by cohesion to maxMembers, remove used members, repeat until no cross-community pair remains); token-optional (shared sub-token = label, else the scope is the label); within-scope spec; Item-2 scope-token exclusion; a token-optional renderDiscoverPrompt variant. (b) LIGHTER — keep the token-anchored builder when scoped, add only Item-2 exclusion + within-scope spec, no semantic-first generator, no prompt variant. (c) PAUSE Phase 5.

**Rationale:** chose (a). It is the designed intent in the mechanism spec and fills the real gap (b) leaves: scoped pools where related facts span sub-communities but share NO single token surface zero bridges under token-anchored enumeration. Reusing the Phase-4 reshape seed-and-grow keeps one cohesion-extraction algorithm and stays deterministic.

**The choice:** full token-optional filtered generator (option a).

STATUS — SHIPPED (corrected 2026-07-22): this fact previously ended at '~5 TDD tasks (Phase 5, Tasks 17-21)', reading as unstarted work. At HEAD the ScopeFilter IS threaded into the builder — `enumerateBridgeCandidates(seeds, clusters, kind, scope ScopeFilter)` and `buildScoredBridges(... scope ScopeFilter ...)` in internal/synthesize/bridge.go — and the reshape machinery it reuses lives in bridge_reshape.go. Note `BridgeSeedSet.Token` is still a plain `string` (not a pointer/omitempty): 'optional' is expressed as the EMPTY STRING, so a consumer must test `Token == ""` for the token-optional case rather than expecting a nil or a missing JSON key.
