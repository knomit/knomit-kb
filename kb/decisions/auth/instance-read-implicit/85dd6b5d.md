---
type: observation
domain: [auth, pki, security, decisions]
confidence: 0.9
sources: 1
entities: [auth.CertGrants, auth.InstancePrincipal, auth.OperatorPrincipal, Server.grants, loopbackGrants, auth.Allowed, pki.IdentityOf, knomit grants]
motifs: [membership-implies-minimum, default-deny-on-error]
refs: ['src://7b4887ce51d9/internal/auth/certgrants.go@ff457f83394b7a47b53ca0017ee9ce6d8f1e7876:0d9467806ed5d9ea2c260833c7e96653f4b83b50', 'src://7b4887ce51d9/internal/auth/certgrants_test.go@ff457f83394b7a47b53ca0017ee9ce6d8f1e7876:d272ea63ab59aa0d1c9effc4b53d2a85a8c8b16d', 'src://7b4887ce51d9/internal/web/server.go@ff457f83394b7a47b53ca0017ee9ce6d8f1e7876:71ed02494485426ab54919bb2c621cf87e6358ee', 'src://7b4887ce51d9/internal/web/auth_enforce_test.go@ff457f83394b7a47b53ca0017ee9ce6d8f1e7876:285455e23fe43d9301d5d45b01819e9ec07d1ab9', 'src://7b4887ce51d9/internal/web/auth_middleware.go@ff457f83394b7a47b53ca0017ee9ce6d8f1e7876:fba38078d92bed81406207d1c7ea1622334582c0', 'kb://3ec012f5b4d2/kb/invariants/auth/one-principal-one-check/8d5cc79d.md', 'kb://3ec012f5b4d2/kb/decisions/pki/role-in-uri-san/429578e0.md']
---
# An instance certificate that chains to the fleet root holds `read` IMPLICITLY and nothing else — the chain is admission — while every further permission, and everything an operator certificate holds, is a grants row keyed by the full-fingerprint principal

DECIDED (master ruling 5, F19 phase 2, 2026-09-22). A certificate chained to the fleet root is fleet ADMISSION — it replaces F10's admitted-fingerprint list — so an instance that presents one holds `read` with no grants row. It holds nothing else by default: `write`, `push:own`, `merge:main`, `operator`, `admin` are grants rows for `instance:<64-hex fp>@cert`, added with `knomit grants add`.

HOW, and why this shape: `auth.CertGrants{Store}` returns `{read}` merged with the store's rows for `KindInstance` + `ViaCert` + non-empty ID, and the store's rows alone for everyone else. `Server.grants()` builds `loopbackGrants{anon: …, store: CertGrants{Store: s.Grants}}`, so BOTH enforcement points (writeGate and the MCP gatePermission/permissionFilter) consult the same answer, and `auth.Allowed` stays the only check (kb/invariants/auth/one-principal-one-check). It is deliberately NOT a shortcut in AuthMiddleware: the middleware decides WHO, never WHAT.

COMPOUND CONDITIONS worth stating in full:
- Only an INSTANCE via a CERTIFICATE gets it. An operator-role certificate maps to `operator:<fp>@cert` (pki.IdentityOf reads the role from the SAN) and holds exactly its rows — no implicit read. `instance:<fp>@token` would not get it either.
- A grants STORE ERROR denies even the implicit read (CertGrants returns the error; Allowed denies), matching default-deny-on-error.
- There is nothing to revoke on this side: a revoked certificate never reaches a Grants lookup because the TLS handshake refused it. Hence `knomit grants revoke <instance> read` is refused with an explanation pointing at `knomit identity revoke`.

CONSEQUENCE a consumer must act on: in phase 2 NOTHING enforces `read` — a request that reached the server has passed the transport boundary — so no HTTP or MCP test can observe a missing CertGrants. `TestServerGrants_WrapTheStoreInCertGrants` pins the wiring directly (sabotage-checked). If a later phase adds the first `read` enforcement point, this implicit read is what keeps enrolled instances from being locked out.

MISREADING TO AVOID: 'the TLS listener serves reads to instances' is true but is not what this fact grants — the TLS listener refuses any request with no certificate principal regardless of grants (never anonymous); CertGrants answers what a principal that got in may DO.
