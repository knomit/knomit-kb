---
type: observation
domain: [config, auth, bridge, unix, decisions]
confidence: 0.9
sources: 1
entities: [config.localListenerName, config.SocketPath, config.Load, Config.Socket, auth.SunPathCap, auth.FallbackSocketDir, syscall.RawSockaddrUnix, knomitapi.SocketPath, KNOMIT_HOME, TMPDIR, XDG_RUNTIME_DIR]
motifs: [environment-dependent-rendezvous, normalisation-before-hashing]
refs: ['src://7b4887ce51d9/internal/config/paths_unix.go@0a275b92e72af75393601df1b7a5ef7ad61bf518:63678d8d94728e1c6ad9c75cb08c8aff4ad0b947', 'src://7b4887ce51d9/internal/config/paths_unix_test.go@0a275b92e72af75393601df1b7a5ef7ad61bf518:2a5d97d32a3809b2facd355cf639bdd916410017', 'src://7b4887ce51d9/internal/auth/socketdir_unix.go@ac3cb8a6c573717615a750d36123edae4a756003:59418f606b603ab4d077efa73e50e8a2802bf943', 'src://7b4887ce51d9/internal/auth/listen_unix_test.go@ac3cb8a6c573717615a750d36123edae4a756003:ab901203632a8ec85fd63191d37830198e435e9b', 'src://7b4887ce51d9/tools/bridge/knomitapi/socketpath_unix_test.go@0a275b92e72af75393601df1b7a5ef7ad61bf518:a67c37a23a70920ac4d4dda476bfbe02ad0aa7b3', 'src://7b4887ce51d9/tools/bridge/knomitapi/dial.go@0a275b92e72af75393601df1b7a5ef7ad61bf518:8835b9a1c287d0b64dd58d717d246f20d39e04fb', 'https://github.com/knomit/knomit/issues/253', 'https://github.com/knomit/knomit/pull/268', 'kb://3ec012f5b4d2/kb/gotchas/windows/pipe-name-case-insensitivity/1d84801b.md', 'kb://3ec012f5b4d2/kb/gotchas/testing/unix-socket-fixtures/33bc383f.md']
---
# A data root too long for sun_path gets /tmp/knomit-<euid>/<8 hex of sha256(Clean(root))>.sock — the LITERAL /tmp, never TMPDIR/XDG_RUNTIME_DIR/os.TempDir, computed in config.localListenerName so server and bridge cannot disagree

knomit#253: a `KNOMIT_HOME` long enough that `<home>/knomit.sock` does not fit in `sockaddr_un.sun_path` made every listen EINVAL, and `knomit serve` was fatal on it.

THE RULE, in `config.localListenerName(home)` on unix: `p := filepath.Join(home, "knomit.sock")`. If `len(p) < auth.SunPathCap()` it returns `p`, byte-identical to before. Otherwise it returns `filepath.Join(auth.FallbackSocketDir(), hex(sha256(filepath.Clean(home)))[:8] + ".sock")`, i.e. `/tmp/knomit-<euid>/xxxxxxxx.sock`, about 30 bytes.
- `auth.SunPathCap()` is `len(syscall.RawSockaddrUnix{}.Path)`: 104 darwin, 108 linux, never typed. `>=` is where bind(2) itself starts refusing on both. A 107-byte path binds on linux and a 108-byte one gets EINVAL, even though Go's `syscall_linux.go` would pass 108 to the kernel. Measured in golang:1.26 linux/arm64, and asked of the kernel on every run by `TestListenLocal_PathLengthBoundaryIsTheSunPathCap`, which is what makes a mistyped cap fail in either direction.
- The server's `config.Load` (`cfg.Socket = localListenerName(cfg.Home)`) and the bridge's `config.SocketPath` (`localListenerName(ResolveHome())`) both come through this ONE function. `knomitapi.SocketPath` only delegates to `config.SocketPath`.

OPTIONS REJECTED (worker D7, reviewer B5/B6, master ruling E3, 2026-09-23):
- `os.TempDir()` or `$XDG_RUNTIME_DIR`: environment-dependent. The bridge can run with a filtered environment (an MCP host spawns it), so the server and the bridge would compute DIFFERENT paths and the bridge would silently fall back to TCP, losing the verified identity. No single-process test can see that.
- Failing the boot on a long root: the old behaviour, which the issue exists to remove.

CONSEQUENCES:
- /tmp is shared, so the directory is used only under the private-dir invariant (0700, owned by euid, checked on both sides). A squatted `/tmp/knomit-<euid>` therefore makes a long-root server serve TCP only, and REFUSE TO BOOT under `[auth].require = true`. That is the intended fail-closed trade. The remedy is a shorter root or an explicit `[socket]`.
- The root is `filepath.Clean`ed before hashing, so `home`, `home/` and `home/.` share a socket. It is NOT made absolute: a relative KNOMIT_HOME already means different roots for processes with different cwds.
- An EXPLICIT `[socket]` / `KNOMIT_SOCKET` bypasses this function. An overlong explicit path reaches `ListenLocal`, which refuses it with `ErrPathTooLong` (WARN, TCP only). The bridge ignores those overrides (pre-existing, not changed here).
- Windows is untouched: a pipe name has no length cap.

WHAT THIS DOES NOT MEAN: not that linux tolerates one more byte than darwin at the boundary. An earlier revision of this fact and of the `SunPathCap` comment claimed linux accepts an unterminated 108-byte path. That was reasoned from Go's syscall check, not measured, and it was false.
