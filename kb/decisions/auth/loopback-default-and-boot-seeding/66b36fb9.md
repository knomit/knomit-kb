---
type: observation
domain: [auth, app, config, security]
confidence: 0.9
sources: 1
entities: [config.AuthConfig, LoopbackDefault, Require, Server.grants, seedOwnPrincipal, auth.LocalPrincipal, auth.SQLGrants.EverGranted, auth.SQLGrants.Grant, auth.SQLGrants.Revoke, grants.revoked_at, TestApp_AuthConfigReachesMiddleware, TestBoot_GrantsOwnUIDLoopbackDefaultOnce]
motifs: [absence-is-not-revocation, convenience-needs-a-guard]
refs: ['src://7b4887ce51d9/internal/app/app.go@e58f08a34a481fa682f798d1b62f84a54c34c058:5d4424b57f36bf48691779fe1e2ba16fff698672', 'src://7b4887ce51d9/internal/config/config.go@e58f08a34a481fa682f798d1b62f84a54c34c058:b51af5a58d5e8ab323fd6b47a9e31c58d3670aba', 'src://7b4887ce51d9/internal/store/migrate/control/000009_grants.up.sql@cec7c5c10b4a752dadbb34a45cb749427923ba8d:4cfde1c461e144f0e00478f7c69ef7e935ab65fd', 'kb://3ec012f5b4d2/kb/invariants/auth/one-principal-one-check/8d5cc79d.md', 'kb://3ec012f5b4d2/kb/invariants/auth/local-principal-has-one-spelling/92b4dd2a.md', 'kb://3ec012f5b4d2/kb/invariants/auth/require-needs-a-bound-listener/0f2335f4.md', 'https://github.com/knomit/knomit/pull/243', 'https://github.com/knomit/knomit/pull/251']
---
# A nil LoopbackDefault falls back to the defaults (a test convenience), and boot seeds the server's own LOCAL principal ONCE PER PERMISSION — asking EverGranted, not For, so a restart never revives what an operator revoked

Two coupled choices about how a fresh knomit instance ends up usable.

ONE: a NIL `Auth.LoopbackDefault` in a `web.Server` literal falls back to `config.Defaults().Auth.LoopbackDefault`. The alternative — nil means deny — was rejected because 73 test files build a `Server` literal with no config, and there is exactly ONE production literal (`internal/app`). Touching 73 files would have bought no security. The COST of the fallback is real and has to be guarded: if the production literal ever stopped carrying `cfg.Auth`, every instance would silently fall back to the full default set and nothing would notice. `TestApp_AuthConfigReachesMiddleware` is that guard — it boots a real App and asserts BOTH halves of `[auth]` arrived, each through the handler. It needs two boots because the halves are mutually exclusive: with `require = true` there is no anonymous principal for a loopback default to apply to. Delete that test and the fallback becomes a hole.

Note the asymmetry deliberately: an EMPTY but present list (`loopback_default = []` in TOML) is honoured as "anonymous holds nothing". Only NIL falls back. An operator who writes an empty list means it.

TWO: boot grants the server's OWN LOCAL principal each permission in `loopback_default`, via `seedOwnPrincipal` (named `seedOwnUID` until knomit#245). That is what makes the local bridge work with zero configuration — it dials the local listener, the OS reports who is calling, and the grant is already there. The OS user running the server is treated as its operator.

THE SEEDED ID IS PER PLATFORM and comes from `auth.LocalPrincipal()`, never built inline: `bridge:uid:<server uid>@socket` on unix, `bridge:sid:<SID>@pipe` on Windows. Building it inline is exactly what broke Windows before knomit#245 — `os.Getuid()` returns -1 there, so boot seeded `bridge:uid:-1@socket` and no request could ever match it. The middleware derives the request's principal from the same per-platform pair; see the local-principal-has-one-spelling invariant.

THE LOAD-BEARING DETAIL: seeding asks `EverGranted`, which counts rows live OR revoked, NOT `For`, which returns only live ones. A revoked row is HISTORY, not absence. Seeding on liveness would mean every restart silently undid an operator's revocation — the permission would look revocable and be unrevokable in practice, which is worse than having no revocation at all. This is why `grants.revoked_at` is a timestamp and a revoke is an UPDATE rather than a DELETE: the row has to survive to say "this was taken away on purpose".

A seed failure STOPS the boot rather than logging. A server that came up without its seed would look healthy and refuse every local write, which is harder to diagnose than not starting. Note that this is one of THREE places the same "do not boot into a 403-everything server" judgement is made — the others are `app.checkLocalListener` and `auth.RequireLocalListener`.
