---
type: observation
domain: [repos, remote, pki, auth, gogit]
confidence: 0.9
sources: 1
entities: [pki.GitScheme, pki.InstallGitTransport, pki.HTTPClient, pki.RewriteEndpoint, client.InstallProtocol, auth method cert, resolveAuthWithOrigin, validateURLAuth]
motifs: [scoped-override-not-global, credential-on-the-transport]
refs: ['src://7b4887ce51d9/internal/pki/gittransport.go@bba1ea54fb47b1c1954edb287685bbf5dbe2dff5:33b720c01a3fa63becb22d8c9e9c4540930d22aa', 'src://7b4887ce51d9/internal/repos/auth.go@bba1ea54fb47b1c1954edb287685bbf5dbe2dff5:35756c91da8814d40f2aa7e92889747f0737de5a', 'src://7b4887ce51d9/internal/web/helpers.go@bba1ea54fb47b1c1954edb287685bbf5dbe2dff5:576eb2a14eb7cc135b4f33e775ed21c0755e4e9c', 'src://7b4887ce51d9/web/src/originAuth.ts@bba1ea54fb47b1c1954edb287685bbf5dbe2dff5:5d057302239a0947530e37ae11cf772217d5df4f', 'https://github.com/knomit/knomit/pull/257', 'kb://3ec012f5b4d2/kb/gotchas/gogit/install-protocol-unsynchronised-and-url-from-endpoint-string/033c56b3.md', 'kb://3ec012f5b4d2/kb/gotchas/pki/openssh-key-is-not-x509keypair-input/1b6e1dda.md', 'kb://3ec012f5b4d2/kb/invariants/pki/no-insecure-skip-without-verifyconnection/a3e656d7.md']
---
# Instance-to-instance git fetches use a custom go-git scheme knomit+https with a rewriting wrapper over pki.HTTPClient, not DNS/IP SANs with go-git's ClientCert/CABundle and not a global https override

Decided in F19 phase 2b (2026-09-22, plan .claude/plans/2026-09-22-f19-phase2b-gogit-mtls.md, master and reviewer rulings). An instance names another instance as its git origin with `knomit+https://<host>:<tls port>/git/<repo>` and auth method `cert`. Every fetch, clone and probe then goes through the phase 2 mutual-TLS client: fleet-root-only chain verification, CRL, URI SAN, named refusals.

OPTIONS CONSIDERED:
(a) Put DNS/IP SANs in instance certificates and use go-git's ClientCert/ClientKey/CABundle options. REJECTED: an address change would force a re-issue (certificates are renewed without re-key and carry only the knomit:// URI SAN). Go's hostname check cannot express the URI SAN. CABundle appends to the SYSTEM pool, so any publicly trusted certificate would verify. And ClientKey goes through tls.X509KeyPair, which cannot read the OpenSSH key without a second on-disk copy (kb/gotchas/pki/openssh-key-is-not-x509keypair-input).
(b) `client.InstallProtocol("https", …)` with the fleet client. REJECTED: process-global, so it would carry every forge (GitHub) clone as well, and the verifier would need a system-verification fallback for non-fleet hosts. That is exactly the kind of fallback the no-insecure-skip invariant forbids.
(c) CHOSEN: a separate scheme, registered ONCE, whose wrapper copies the endpoint with Protocol "https" and delegates to go-git's http transport over pki.HTTPClient. The `https` entry is never touched; `TestGitTransport_HTTPSProtocolEntryUntouched` pins pointer equality.

SHAPE THAT FOLLOWS: `cert` is a fifth remote auth method meaning 'the credential is the client certificate on the transport'. resolveAuth returns no go-git AuthMethod for it. For a knomit+https URL, resolveAuthWithOrigin FORCES cert and errors on an explicit token/basic. The edge (validateURLAuth and the wizard's urlAuthMismatch, same words, same table) refuses mismatched pairs. All scheme checks are case-insensitive because go-git lowercases the scheme.

OUT OF SCOPE BY DESIGN: push (knomit's /git exposes no receive-pack until F11), F09 signature verification on fetch, F10 peers, per-repo trust roots, cross-scheme redirects.

MISREADING TO AVOID: this is not 'knomit replaced go-git's https'. Forge clones use go-git's stock https transport and never see the fleet client.
