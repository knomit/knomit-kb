---
kind: pragmatic
type: policy
domain: [mcp, fact, schema, testing]
confidence: 0.95
sources: 0
entities: [internal/mcp/factschema.go, factTypeDocs, factOriginDocs, TestFactSchema_DescriptionsAreComplete, fact.AllOrigins, fact.AllEpistemicTypes, fact.AllPragmaticTypes, knomit_learn, knomit_update]
motifs: [parallel-implementations-diverge]
refs: ['src://knomit/internal/mcp/factschema.go@5437ab6c', 'src://knomit/internal/mcp/factschema_test.go@5437ab6c', 'src://knomit/internal/fact/origin.go@5437ab6c']
---
# Fact-schema enums derive from internal/fact; their DESCRIPTIONS live in internal/mcp and a completeness test is what keeps them in sync

The knomit_learn / knomit_update JSON schemas and the server instructions all read from ONE place. Split of ownership, deliberate:

- ENUM VALUES derive from internal/fact — AllEpistemicTypes(), AllPragmaticTypes(), AllOrigins(). internal/fact is authoritative for what exists.
- DESCRIPTIONS live in internal/mcp/factschema.go (factTypeDocs, factKindDocs, factOriginDocs). internal/fact stays FREE OF PROSE.

**Why descriptions do not belong in internal/fact.** Their wording is tuned for one audience — LLM tool-callers reading a JSON schema — and is driven by prompt-engineering concerns, not domain semantics. Putting them in the domain package would mean editing internal/fact for prompt-tuning reasons, which is the wrong coupling. Do NOT 'consolidate' them into internal/fact later; that is a regression, not a cleanup.

**The completeness test is the load-bearing part, not the shared table.** TestFactSchema_DescriptionsAreComplete asserts every member of the authoritative sets has a description, AND that no description exists for a value internal/fact no longer knows about (dead prose still shown to agents). Without it this design degrades straight back to drift: someone adds a type to internal/fact and the schema silently omits it, which is precisely how the contract came to be defined in three places with a fourth copy in update.go. A shared table alone does not enforce anything.

**Consequence:** adding a Type or Origin to internal/fact WILL fail the mcp test until you add its description. That failure is the design working, not an obstacle — the message names the file and the exact entry to add.

**What this does NOT mean:** query.go's type/origin filter descriptions are deliberately NOT routed through this table. They are illustrative ('e.g. observation, policy') and describe filtering semantics rather than the vocabulary; forcing exhaustive 12-item lists into a filter description reads worse.
