---
kind: pragmatic
type: policy
domain: [mcp, web, repos, sessions, invariant]
confidence: 0.95
sources: 1
entities: [binding_handles, handle_experiments, Mcp-Session-Id, SessionBindingMiddleware, resolveHandle, repos.SessionScoped, repos.RequireBinding, knomit_bind, knomit_repos, TestSessionBindingMiddleware_ResolvesNothingFromTheSessionID, TestSessionBindingMiddleware_ResolvesNoExperimentFromTheSessionID]
motifs: [identifier-cannot-identify, ignoring-contradicts-caller-intent]
refs: ['kb://3ec012f5b4d2/kb/incidents/mcp/session-binding/cross-binding-overwrite/17935f4f.md', 'kb://3ec012f5b4d2/kb/invariants/mcp/binding-gate/registration-seam/8cdea548.md', 'kb://3ec012f5b4d2/kb/invariants/mcp/client-sessions/98c87890.md', 'kb://3ec012f5b4d2/kb/decisions/mcp/experiments/handle-keyed-sibling-table/d0f37357.md', 'kb://3ec012f5b4d2/kb/architecture/web/lens-experiment-mount/ca9f9e4e.md', 'kb://bc6eac5f37df/kb/invariants/ai/agents/tools/mcp/security/sessions/b471f9ef.md', 'src://7b4887ce51d9/internal/mcp/binding_gate.go@b3cebeb4d6255ec5e9c1e58498dae1c106e64932:efb55bbb42021c4b07fd3e399069a07f6672c91f', 'src://7b4887ce51d9/internal/web/session_binding_middleware.go@b3cebeb4d6255ec5e9c1e58498dae1c106e64932:8d3efaebb7a4fa935f7c9aa326f2128858904e44', 'src://7b4887ce51d9/internal/mcp/binding_gate.go@55cd390dcdfbad97f9d5f6774cfab66ff783bd2e:4fd8e8ddba5915d85e9b21376d0f526c02d31353', 'src://7b4887ce51d9/internal/web/session_binding_middleware_test.go@55cd390dcdfbad97f9d5f6774cfab66ff783bd2e:622778988d34f9cdd29ebd462174238bd8dfe043']
---
# On the unscoped MCP mount NOTHING resolves a binding from Mcp-Session-Id — the call's own `binding` handle is the only router, an unknown handle hard-fails, and a `binding` on a URL-scoped mount hard-fails rather than being ignored

Three rules, and all three are load-bearing together. Dropping any one of them reopens the 2026-09-17 cross-binding incident.

1. NO SESSION LOOKUP. On the unscoped mount `/api/v1/mcp`, `web.SessionBindingMiddleware` does exactly two things: mark the context session-scoped, and install a `repos.PinRecorder`. It does NOT read `Mcp-Session-Id` and does NOT resolve a binding. A client may share one connection — and so one session id — across several independent logical jobs, so any state keyed on that id serves whichever job bound LAST.

2. REQUIRED HANDLE, HARD FAIL. Every tool except `knomit_repos` and `knomit_bind` requires the opaque handle as its `binding` argument on this mount. Missing ⇒ an error naming `knomit_bind` and the argument. Unknown ⇒ exactly `unknown binding handle — call knomit_bind`, which deliberately does NOT echo which handles exist: the handle is the only thing separating two callers on one connection, so enumerating live ones would hand one caller another's routing. There is no fallback of any kind — not to a session, not to a default repo, not to "the only repo".

3. URL-SCOPED REFUSES ON PRESENCE. When `repos.SessionScoped(ctx)` is false the repo comes from the path, and a `binding` argument is refused with `this endpoint is bound by its URL; do not pass binding`. Refused on PRESENCE, so an empty string is refused too. Ignoring it would be worse than failing: the caller believes it selected a base while the URL serves a different one — the exact silent-misroute shape the handle exists to remove.

THE HANDLE NOW ROUTES A THIRD THING (PR #236). Alongside the pin and the read branch it carries the caller's ACTIVE EXPERIMENT, in the sibling table `handle_experiments`, read next to the handle in `resolveHandle`. The three rules above are unchanged and apply to it unaltered — in particular rule 1: the experiment is keyed on the handle and NEVER on the session id, for the same reason and with a worse failure, since an experiment and its parent share an ontology by construction and a misrouted write would commit silently. Rule 3 extends to the new `/lenses/{lens}/experiments/{name}/mcp` mount, which is URL-scoped and refuses a `binding` on presence like every other. Do not read "pin plus branch" as the complete list of what a handle decides.

ENFORCING CODE: `resolveHandle` and the gate in `internal/mcp/binding_gate.go`; `internal/web/session_binding_middleware.go` for rule 1. Guarded by `TestSessionBindingMiddleware_ResolvesNothingFromTheSessionID`, which mints a live handle and then uses that handle's own value AS the session id — the most favourable case any session-keyed lookup could have — and asserts no Binding appears; and by its sibling `TestSessionBindingMiddleware_ResolvesNoExperimentFromTheSessionID`, which does the same with a handle that is genuinely inside an experiment.

SCOPE, since several neighbouring facts cover adjacent ground and none of them states this rule: WHERE the gate must be installed is the registration-seam invariant; WHICH tools exist and which are gated is the tool catalogue; WHY the unscoped mount exists at all is the binding decision; WHY `knomit_repos` alone needs no handle is the discovery-tool decision. This fact is only about what may and may not decide the TARGET of a call.

WHAT THIS DOES NOT MEAN. It is not an authorization boundary: knomit has no authentication, anyone who can reach the endpoint can already reach every repo by URL, and a handle grants nothing new. The handle is unguessable only so a model cannot conjure a valid one from a repo name it has seen. It also does not constrain the URL-scoped mounts themselves, which keep resolving from the path exactly as before.
