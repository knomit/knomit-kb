---
kind: pragmatic
type: policy
domain: [web, ui, temporal, explain]
confidence: 0.9
sources: 1
entities: [web/src/TimelineNav.tsx, web/src/LeftPanel.tsx, web/src/useTimeTravel.ts, onOpenFileAt, activeCommit, timeline-file-row, selectAnchorCommit, APPLY_NAV]
refs: ['https://github.com/knomit/knomit/pull/77', 'src://7b4887ce51d9/web/src/TimelineNav.tsx@4baae4ed3043cbcb37a797b625f5e8a1a071f912:d51b23711644ad96d2b7397f09e00c034931fe4d', 'src://7b4887ce51d9/web/src/LeftPanel.tsx@4baae4ed3043cbcb37a797b625f5e8a1a071f912:36127a5d9e402e39c989456136025e99459b7250', 'src://7b4887ce51d9/web/src/useTimeTravel.ts@4baae4ed3043cbcb37a797b625f5e8a1a071f912:4d99d63824e2d08833ccaaf52752e2c261995395']
---
# Timeline 'files affected' clicks navigate to the VIEWED (active) commit, not the open fact's anchor commit

In the history timeline (LeftPanel -> TimelineNav), a click on a 'files affected' row MUST navigate to the commit whose changeset that file belongs to — the viewed/active commit shown in the panel — NOT the currently-open fact's anchor. TimelineNav renders the file rows from the detail of `activeCommit` and calls `onOpenFileAt(f.path, activeCommit)`; LeftPanel passes `activeCommit={selectAnchorCommit(state)}`; `useTimeTravel.openFileAt(path, commit)` dispatches `APPLY_NAV` with `asOf:{ mode:'history', commit }`. Self rows and deleted-file rows are disabled (`isDisabled = isDeleted || isSelf`), so navigation only targets a commit where the file provably exists.

The bug (PR #77): the old implementation navigated to `fact.commit_hash` (the open fact's anchor). When the viewed commit differed from the anchor — the panel fell back to the latest version, or you browsed across commits — the sibling file often did not exist at the anchor, so the fact read 404'd (and /incoming /outgoing 500'd/404'd).

WHAT THIS DOES NOT MEAN: the original ExplainView.tsx / FactHistoryPanel.tsx / onFileClick / history-file-row named in this fact's earlier revisions NO LONGER EXIST — they were deleted in commit 0074f640 ('unify now/history into one surface; delete ExplainView overlay'). Do not look for onFileClick or a history-file-row test id; the current surface is TimelineNav.tsx with onOpenFileAt and data-testid='timeline-file-row'. The INVARIANT (anchor to the viewed commit, never the fact's commit_hash anchor) is unchanged; only the components moved. (Two dead src://...@a1ef27c refs to the deleted files remain in this fact's ref history as provenance only.)
