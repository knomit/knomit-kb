---
kind: pragmatic
type: policy
domain: [store, synthesize, search]
confidence: 0.92
sources: 1
entities: [ScopeFilter.Matches, canonicalizeDomain, domainTokens, stemDomainToken, fact_domain_tokens, fact_entities, SearchOptions.Domain, SearchOptions.DomainExact]
refs: ['src://knomit/internal/store/domain_match.go@db0455c', 'src://knomit/internal/synthesize/effort.go@db0455c', 'src://knomit/internal/store/migrate/repo/000008_fact_domain_tokens.up.sql@db0455c', 'src://knomit/internal/synthesize/effort.go@2a9689388fb4025e7f169e19031a02b6292e1750', 'src://knomit/internal/store/domain_match.go@2a9689388fb4025e7f169e19031a02b6292e1750']
---
# All domain/entity comparisons MUST use the canonical tokenized fuzzy-matcher (canonicalizeDomain/domainTokens/stemDomainToken), never raw string equality

Any code that decides whether a fact's domain or entity tags match a caller-supplied domain/entity MUST route through the shared canonical/tokenized matching machinery in internal/store/domain_match.go — NEVER raw string comparison (==, slices.Contains, map lookup on the authored tag).

The machinery and what it normalizes:
- canonicalizeDomain: NFC -> Unicode case-fold -> de-hyphenize (so 'Remote-Sync' == 'remote sync'). Case-insensitivity is non-negotiable; the fact_domains/fact_entities junction tables are COLLATE NOCASE for the same reason.
- domainTokens: splits on whitespace AND '/', so hierarchical tags are token-searchable ('store/sqlite' matches 'sqlite').
- stemDomainToken: singularizes via go-pluralize ('migrations' == 'migration', 'indices' == 'index'), with len<=3 and -ics guards.
- fact_domain_tokens provides containment matching in SQL; SearchOptions.Domain (default, token-containment) vs DomainExact (opt-in exact) is the canonical query surface.

WHY load-bearing: raw matching makes the SAME domain/entity argument select DIFFERENT fact sets depending on which code path handles it, and silently returns empty/partial pools on any case/hyphen/plural/hierarchy mismatch. A user passing domain=['Store'] or ['migrations'] or ['store/sqlite'] expects the same facts knomit_query would return; a raw comparator misses all three.

ENFORCEMENT POINT (was a violation, FIXED as of HEAD 2a96893): synthesize.ScopeFilter.Matches (internal/synthesize/effort.go) is the scope filter for knomit_review/knomit_hypothesize. It PREVIOUSLY used slices.Contains (raw, case-sensitive) but NOW routes domains through store.DomainTagMatches and entities through store.EntityTagMatches (effort.go), so scoped review/hypothesize pools match the same semantics as knomit_query. Do not 'fix' this code — it is already correct; and there is no live slices.Contains bypass on domain/entity tags anywhere in internal/synthesize or internal/store. This is the canonical reuse pattern for in-memory matching: the canonicalize/token/stem functions are pure-Go and reusable, so there is never an in-memory-vs-SQL excuse to fall back to raw comparison.

WHAT THIS DOES NOT MEAN: 'entities are tokenized like domains' — they are NOT. EntityTagMatches is case-folded EQUALITY ONLY (mirrors the COLLATE NOCASE fact_entities junction); there is NO entity token table, no stemming, no slash-split or whitespace-split for entities. Use DomainTagMatches for domains and EntityTagMatches for entities; do NOT assume domain token-containment semantics (hierarchy '/', plural stemming) apply to entity tags. Related: kb/decisions/architecture/synthesize/scope-filter, kb/architecture/store/domain-token-containment-matching.
