---
kind: pragmatic
type: policy
domain: [store, repos, sync, git, invariant]
confidence: 0.95
sources: 1
entities: [Service.AdvanceLocalUpstream, runLocalReconcileLoop, hasOrigin, MainReconcileResult, ModeNoop, ModeFF, Service.SetOnCommit]
motifs: [fail-safe-means-skip, paired-loops-mutually-exclusive]
refs: ['src://7b4887ce51d9/internal/store/local_reconcile.go@3df17f54b6f6ae29984d43b926c500a834f84831:6fecf0aa58928000d80080cfbf85cf49e44b6193', 'src://7b4887ce51d9/internal/repos/sync.go@3df17f54b6f6ae29984d43b926c500a834f84831:6443a257958c0f30d9f0dbe0ffa35d0cd15984ea', 'kb://3ec012f5b4d2/kb/gotchas/repos/remote-sync/ed75e605.md', 'kb://3ec012f5b4d2/kb/invariants/store/notify-commit-single-chokepoint/8bccb67c.md', 'https://github.com/knomit/knomit/pull/205']
---
# The local reconcile only ever FAST-FORWARDS the consensus branch and only ever SKIPS on doubt: diverged history is warn-and-skip, upstream == agent branch is a no-op, an unreadable origin row skips one tick and decides nothing — it never resets a branch and never exits the loop on an error

Rules for AdvanceLocalUpstream (internal/store/local_reconcile.go) and runLocalReconcileLoop (internal/repos/sync.go), PR #205:

1. Advance only when refs/heads/<upstream> is an ancestor of the agent tip (fast-forward). If it is not, log at warn and return ModeNoop. NEVER SetReference to a non-descendant. The recorded force-reset incident (kb/gotchas/repos/remote-sync/ed75e605) is the reason: reconcile acting destructively on a branch assignment cost data once.
2. upstream == agentBranch, or either empty, is a no-op — the degenerate config from that same incident.
3. The origin read that decides whether this loop or the origin-backed reconcile loop owns the repo is THREE-STATE: origin present (this loop exits, the other owns it), origin absent (this loop runs), UNREADABLE (skip this tick, decide nothing, log). A first version exited the loop permanently on any error, so one SQLITE_BUSY silently stopped main from advancing for the life of the process, indistinguishable from a quiet repo; the review gate caught it. Matches the sibling loop's fresh-config-per-tick convention.
4. Every advance goes through notifyCommit under the branch lock (single chokepoint). Nothing chains into Service.SetOnCommit — that single observer slot belongs to the SSE broadcast; polling is the whole trigger.
5. Errors in a tick are logged with the repo name and retried next tick; the loop ends only on ctx cancellation or a definite "origin exists".

Misreadings: "never reset" is NOT "never move" — fast-forward is the normal case. "Skip on unreadable" is NOT "treat unreadable as no origin" — that would run this loop against a repo the origin loop also owns.
