---
kind: pragmatic
type: policy
domain: [oauth, pki, security]
confidence: 0.9
sources: 1
entities: [oauth.ApprovalStatement, ApprovalStatement.Bytes, oauth.SignStatement, oauth.SignedStatement, Issuer.ApplySigned, oauth.RequestDigest, POST /oauth/approve, 'GET /oauth/pending/{id}', knomit oauth approve --sign, knomit oauth approve --signed, root.crt, '[tls].dir', ErrNoFleetRoot, ErrBadSignature, ErrWrongInstance, ErrStatementExpired, ErrStatementMismatch, ErrReplayed, ErrMalformedStatement]
motifs: [domain-separated-signature, rebuild-then-verify, sign-what-you-saw]
refs: ['src://7b4887ce51d9/internal/oauth/signedapprove.go@52176c9f3207df73a085430915d46f9b8f731c29:a1fa44a3ba22d6a6274d9f19ba06bd0e390a69fd', 'src://7b4887ce51d9/internal/oauth/signedapprove_test.go@52176c9f3207df73a085430915d46f9b8f731c29:23b6a6e39c1a7e4bf0adb1f9ef1b52617806cf27', 'src://7b4887ce51d9/internal/oauth/describe.go@52176c9f3207df73a085430915d46f9b8f731c29:ee07486a47203a72a2be8cec6c825ca326d0ee92', 'src://7b4887ce51d9/internal/oauth/describe_test.go@52176c9f3207df73a085430915d46f9b8f731c29:aa0d179cab58a8bad019af888a125568d0eb352b', 'src://7b4887ce51d9/cmd/oauth_sign.go@52176c9f3207df73a085430915d46f9b8f731c29:3ba2c3dd97eef33c96a1547c0e11d09a972eae67', 'src://7b4887ce51d9/cmd/oauth_sign_test.go@52176c9f3207df73a085430915d46f9b8f731c29:6689801c62a9600275fb8ccabe9e7c6bcddd35f9', 'src://7b4887ce51d9/internal/app/app.go@52176c9f3207df73a085430915d46f9b8f731c29:042a7469829b09faa04974d7fff39ef1965272a4', 'src://7b4887ce51d9/internal/app/oauth_wiring_test.go@52176c9f3207df73a085430915d46f9b8f731c29:ec4d054b5c45cf0a6c3922e35a20488a18137313', 'kb://3ec012f5b4d2/kb/decisions/oauth/consent-path-1/7ac59e2a.md', 'kb://bc6eac5f37df/kb/gotchas/ai/agents/tools/mcp/security/client-identity/066a9dad.md', 'https://github.com/knomit/knomit/pull/283']
---
# A master-key OAuth approval signs ONE canonical, domain-tagged, instance-bound statement — first line knomit-oauth-approve/v1 or knomit-oauth-deny/v1, then fingerprint, pending id, request digest, [subject, sorted scopes,] expiry, newline-joined with no trailing newline — and the verifier REBUILDS those bytes from validated fields, never verifying client-supplied bytes

THE BYTES (ApprovalStatement.Bytes, the only encoding):
- approve: `knomit-oauth-approve/v1`, <instance fingerprint>, <pending id>, <request digest>, <subject>, <scopes comma-joined, sorted>, <expires unix>
- deny: `knomit-oauth-deny/v1`, <instance fingerprint>, <pending id>, <request digest>, <expires unix>
Lines are joined with \n, and there is NO trailing newline.

Every field is validated before joining, so none can contain a separator:
- the fingerprint is 64 lowercase hex (pki.Fingerprint of the instance key);
- the id has a pending id's shape;
- the digest is RequestDigest's 64 hex;
- the subject matches subjectRE;
- scopes are grantable permission names (never admin), non-empty, without repeats;
- expires is a positive int formatted by strconv, so there is no sign and no leading zero;
- a deny carries no subject and no scopes.

WHY EACH CHOICE:
- **The first line is the domain separation (reviewer S2).** The same master key signs X.509 TBS structures and CRLs, which are DER and start with 0x30. A tagged text statement can never verify as either, nor they as it. This is the rule for EVERY non-X.509 use of the master key: sign a message whose first line is a `knomit-<purpose>/v<n>` tag, and require that exact tag when verifying. Phase 4's master-signed fleet policy will be the second user.
- **The verb lives in the tag.** An approve signature never verifies as a deny, nor the reverse.
- **Binding the fingerprint** stops a statement for one headless instance from working on another.
- **Binding the request digest (ruling R4).** The digest covers the requester-supplied fields: client_id, client_name, redirect_uri, resource and requested scopes. The operator signs over what they were SHOWN. The instance recomputes the digest from its own pending row and answers ErrStatementMismatch (409) when a relay showed the operator a different request.
- **Rebuild, then verify (S1).** SignedStatement carries the FIELDS plus a base64 signature. ApplySigned rebuilds the bytes and verifies over what it rebuilt, so there is no parse-after-verify gap. The JSON's `issuer` field is only a delivery address and is not signed.

VERIFICATION ORDER in Issuer.ApplySigned, each step with its named error:
1. malformed (400);
2. fleet root: <[tls].dir>/root.crt, read PER STATEMENT, so enrolment after boot works. Absent means ErrNoFleetRoot (503, never a 500): an unenrolled instance cannot do signed approval.
3. ed25519 signature (ErrBadSignature, 403);
4. own fingerprint (ErrWrongInstance, 403);
5. window, now − 5m < expires ≤ now + 10m + 5m, with the skew between two machines' clocks (ErrStatementExpired, 403);
6. replay table (ErrReplayed, 409);
7. pending row: unknown is 404; ErrStatementMismatch is 409;
8. then Approve or Deny, recorded as decided_by and granted_by `master:<first 8 hex of the root fingerprint>`. That string is deliberately not a principal spelling, so it can never match a grants row.

OPERATOR SIDE (`knomit oauth approve|deny <id> --sign <issuer> --dir <master> --instance <fp>`):
- The instance fingerprint is the OPERATOR's input (ruling W2). It must appear in the master's issued.jsonl with a serial not in revoked.jsonl, and is never taken from the public description.
- The command fetches GET <issuer>/oauth/pending/<id>, recomputes the digest from the fetched fields (and refuses one that does not match), and prints everything %q-escaped with the redirect HOST first before signing.
- `--passphrase-file -` reads the passphrase AFTER the printout, which is the confirmation. A passphrase file needs --yes.
- Without --scopes it signs oauth.DefaultCeiling (D13) explicitly, so the statement always names what it grants.

MISREADING TO AVOID: the signature proves the MASTER approved; it does not identify the client program. The operator still judges the request from the printed, requester-supplied fields, which is why the redirect host is printed first.
