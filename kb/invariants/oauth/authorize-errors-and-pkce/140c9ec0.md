---
kind: pragmatic
type: policy
domain: [oauth, security]
confidence: 0.85
sources: 1
entities: [Issuer.authorize, redirectWith, Client.RedirectAllowed, resourceUnder, challengeRE, verifierRE, ScopesSupported, RFC 9207, RFC 8252, RFC 8707]
motifs: [validate-before-redirect, missing-advertisement-fails-open]
refs: ['src://7b4887ce51d9/internal/oauth/issuer.go@d813eb8c5c06b66998aef2bf0d1b2a6395ad8bf1:1a832cb78669080db7df94e8377b26888a45e667', 'src://7b4887ce51d9/internal/oauth/audience.go@d813eb8c5c06b66998aef2bf0d1b2a6395ad8bf1:83f48f7a70b21c23567c6df59552a191ad99fdb1', 'src://7b4887ce51d9/internal/oauth/issuer_test.go@d813eb8c5c06b66998aef2bf0d1b2a6395ad8bf1:73d0ab2421cd607fb8f3c443a2a27f5dd1eb591a', 'kb://bc6eac5f37df/kb/invariants/ai/agents/tools/mcp/security/authorization/pkce/ce7e0330.md', 'kb://bc6eac5f37df/kb/invariants/ai/agents/tools/mcp/security/authorization/mix-up/bdb36fce.md']
---
# /oauth/authorize answers a PAGE, never a redirect, until client_id and redirect_uri have validated; after that every error goes to the redirect_uri with state and iss — and PKCE S256, a resource under the issuer, and permission-name scopes (no admin) are mandatory

ORDER in Issuer.authorize: resolve client_id, then check redirect_uri against it. Any failure there is a 400 HTML page: redirecting to an unvalidated URI would make the issuer an open redirector. Only then do errors travel to the redirect_uri, ALWAYS with iss = the issuer (RFC 9207 — MCP's mix-up defence fails open without it; kb://bc6eac5f37df/kb/invariants/ai/agents/tools/mcp/security/authorization/mix-up/bdb36fce.md) and with state when the client sent exactly one. A repeated parameter is invalid_request (a repeated OPTIONAL one must not be silently dropped and the request parked as if it had not been sent); a repeated client_id or redirect_uri reads as empty and is refused as a page.

REDIRECT MATCHING (Client.RedirectAllowed): exact string, except a registered http://127.0.0.1/… or http://[::1]/… accepts any port (RFC 8252 §7.3) with scheme, host, path and query still exact. localhost is NOT a port wildcard.

REQUIRED: response_type=code; code_challenge_method=S256 (absent or plain refused — OAuth 2.1 would default absent to plain); a 43-char base64url challenge; resource that is the issuer or a canonical URL under it (resourceUnder: issuer origin byte-for-byte, no userinfo/query/fragment/escapes/'..'/'//', not a bare trailing '/', path at a '/' boundary under the issuer path — a same-length foreign host such as 127.0.0.2 for 127.0.0.1 is refused); every scope in ScopesSupported (read, write, push:own, merge:main, operator — admin is never grantable to a token). Absent resource is invalid_target, not defaulted.

TOKEN ENDPOINT: the code is SPENT before client_id, redirect_uri, the S256 verifier (43–128 unreserved chars, constant-time compare) and resource are checked, so a failed exchange burns it and a verifier cannot be guessed at; a spent code presented again revokes its family. Refresh checks client, resource (absent = the family's; different = invalid_target) and scope (subset of the family's ceiling, never wider) BEFORE rotating. Responses carry Cache-Control: no-store.
