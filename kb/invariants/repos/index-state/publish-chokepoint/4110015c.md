---
kind: pragmatic
type: policy
domain: [repos, index, sse, web, invariant]
confidence: 0.95
sources: 1
entities: [RepoInstance.publishIndex, markIndexing, markIndexReady, markIndexFailed, setIndexProgress, IndexEvent, IndexHub, TaskHub, Manager.openOne, indexCtx]
motifs: [bypass-defeats-guarantee, announcement-rides-the-change]
refs: ['kb://3ec012f5b4d2/kb/incidents/web/index-chip-stale-after-heal/1368e90f.md', 'kb://3ec012f5b4d2/kb/architecture/repos/lifecycle/fa0ea608.md', 'kb://3ec012f5b4d2/kb/invariants/store/notify-commit-single-chokepoint/8bccb67c.md', 'kb://3ec012f5b4d2/kb/decisions/repos/create-job/done-means-indexed/0c1b3368.md', 'src://7b4887ce51d9/internal/repos/index_events.go@594b551a5f3ed6192044a76935a5dcf84e882968:607fdff34c9d12f7d8978286e0d7c4795b0b2220', 'src://7b4887ce51d9/internal/repos/instance.go@594b551a5f3ed6192044a76935a5dcf84e882968:9612f53921e70085a776c3264e5a6e8091831860', 'https://github.com/knomit/knomit/pull/219']
---
# Index-state events are published INSIDE markIndexing/markIndexReady/markIndexFailed and nowhere else — never at a call site — so "every exit path marks" and "every exit path announces" are ONE rule that cannot drift apart

Every index-state change publishes its own event, from inside the three mutators. `RepoInstance.publishIndex` is the only thing that constructs or broadcasts an `IndexEvent`, and the three mark* methods are the only things that change `indexState`. Do not publish from a call site, and do not add a fourth mutator.

WHY A CHOKEPOINT. Index state changes in five places today, ALL inside `Manager.openOne` — the synchronous `DisableBackgroundSync` branch's two exits, and the background heal's `markIndexing` plus its two exits — and a sixth will be added by someone who does not know this event exists. Publishing on the STATE CHANGE rather than at the sites that cause it makes announcing a property of the change. This is the same shape as `notify-commit-single-chokepoint` in the store, and the same motif: a bypass would defeat the guarantee, so there is no bypass to take.

IT INHERITS THE STUCK-INDEXING GUARANTEE, and this is the load-bearing part. The 2026-08 incident was a heal exit path that reached neither mark, pinning the UI at `indexing` forever; the repair made every exit path mark. Because the event rides ON the mark, that existing rule now carries the new one for free — there is no second "and remember to publish" rule to keep in sync with it, and any future work that preserves the first automatically preserves the second.

THE CANCELLATION EXIT DELIBERATELY MARKS NOTHING, and therefore publishes nothing. The heal goroutine's `if b.indexCtx.Err() != nil { return }` is a clean shutdown — the instance is being torn down and its status is no longer observed. That is by design, not a missed emit, and a reader auditing "every exit announces" must not add a mark there to close the apparent gap: marking on teardown would publish a state for an instance that is going away.

PROGRESS IS THE ONE THROTTLED CASE. `setIndexProgress` publishes at most once per repo per second; the three mark* calls are never throttled, and a terminal also RESETS the throttle window so a following progress event is not suppressed by a now-irrelevant tick. The structure is what makes a terminal unswallowable — `throttled` is true only on the progress path, and terminals take the other branch — rather than a timing exemption someone can get wrong later.

WHAT THIS DOES NOT MEAN: it is not a claim that every index WORK change is announced. It covers every change to the index STATE CELL. An operation that does index work without touching that cell publishes nothing, which is a property of that operation and not a hole in this rule.
