---
kind: pragmatic
type: policy
domain: [repos, remote, pki, security, auth]
confidence: 0.9
sources: 1
entities: [pki.InstallGitTransport, pki.ErrNotEnrolled, fleetSource, checkFleetEndpoint, app.installGitTransport, remoteAuthFromRecord, resolveAuthWithOrigin, InsecureSkipTLS]
motifs: [bypass-defeats-guarantee, fail-closed-registration]
refs: ['src://7b4887ce51d9/internal/pki/gittransport.go@efc0318e167131ae7622d22c91121b3f04ff80e3:1c0e3ed91dfe89cbef7c429750746e1f6d71a95f', 'src://7b4887ce51d9/internal/pki/gittransport_test.go@efc0318e167131ae7622d22c91121b3f04ff80e3:a74d50bd3c0f003910bbb0b361f4730e0ef381b0', 'src://7b4887ce51d9/internal/app/app.go@efc0318e167131ae7622d22c91121b3f04ff80e3:c89a663ba5d50eb0bd465225145eb8ebc8fb67cc', 'src://7b4887ce51d9/internal/app/gittransport_test.go@efc0318e167131ae7622d22c91121b3f04ff80e3:6633b4ba0cbd6c9ffe8758086963e316b789a07e', 'src://7b4887ce51d9/internal/repos/auth.go@efc0318e167131ae7622d22c91121b3f04ff80e3:35756c91da8814d40f2aa7e92889747f0737de5a', 'src://7b4887ce51d9/cmd/fleet_origin_test.go@efc0318e167131ae7622d22c91121b3f04ff80e3:5f863d40d8dc03275430e90c4188cdc6fba4f276', 'kb://3ec012f5b4d2/kb/invariants/repos/remote/ssh-host-key-verification/4d6725e4.md', 'kb://3ec012f5b4d2/kb/invariants/pki/no-insecure-skip-without-verifyconnection/a3e656d7.md']
---
# A knomit+https origin is ALWAYS served by the fleet transport or fails with a named error before any dial: not enrolled is ErrNotEnrolled, unusable pki files are an error, and nothing ever falls back to plain https, an unverified client or a forge credential

The HTTPS twin of kb/invariants/repos/remote/ssh-host-key-verification (which forbids falling back to InsecureIgnoreHostKey).

THE RULE, in full:
1. The scheme is ALWAYS registered. `app.New` calls `pki.InstallGitTransport(cfg.TLS.Dir, keyPath)` whether or not the instance is enrolled, before repos.New and Manager.Start. That covers both boot paths, since the desktop also calls app.New. Without the registration go-git would answer 'unsupported scheme', a different failure, and a later edit could 'fix' that by mapping the scheme onto plain https.
2. The client is resolved at EACH session from the files under [tls].dir. No instance.crt means `ErrNotEnrolled`, with no connection attempted (TestGitTransport_NotEnrolledFailsNamedWithoutConnecting uses a listener that records any accept). A certificate without its root or CRL, or for another key, is an error, also before any dial. There is never a transport that connects without pki.ClientConfig's verifier.
3. Enrolment after boot needs no restart. The source rebuilds when the instance.crt, root.crt or crl.pem bytes change, and a rebuild closes the old client's idle connections.
4. No forge credential reaches a peer, through four layers, any one of which suffices: remoteAuthFromRecord gives a fleet origin nothing from the global [remote] config; resolveAuthWithOrigin forces cert and errors on an explicit token/basic; the transport refuses a non-nil go-git AuthMethod and URL userinfo; and the transport hands go-git nil auth. The cmd e2e asserts no Authorization header on the TLS listener, and only goes red with all four removed.
5. go-git's TLS options (ClientCert, ClientKey, CABundle, InsecureSkipTLS) and proxies are REFUSED on this scheme, because go-git would otherwise clone the http.Transport and edit its tls.Config.

CONSEQUENCE for anyone touching it: do not add a 'not enrolled yet, try plain https' path, do not map knomit+https onto https in the URL layer, and do not return a transport from a failed source. Each of these turns a named failure into an anonymous or unverified fetch.

MISREADING TO AVOID: 'always registered' does not mean 'always works'. It means the failure is always OURS and always named. Also, 'the fleet port needs no network boundary' is about authentication only; revocation is checked at the handshake, so a kept-alive connection outlives it until the server's idle timeout (a known phase 2 server-side gap, tracked separately).
