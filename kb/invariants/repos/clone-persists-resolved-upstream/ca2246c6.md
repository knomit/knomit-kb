---
kind: pragmatic
type: policy
domain: [repos, store, remote, clone, invariant]
confidence: 0.95
sources: 1
entities: [Manager.initClone, Service.InitFromRemote, Service.initFromEmptyRemote, SetRemote, upstreamMain, OriginSpec.Branch, repoBuilder.rehydrateUpstreamMain, repoBuilder.setupIndex]
refs: ['src://7b4887ce51d9/internal/repos/lifecycle.go@0df3b891b86627bcce901c448974ad9cc99deb10:2edd77e3d3ff69c910e3439472b42c16ba6a940b', 'src://7b4887ce51d9/internal/store/repo.go@0df3b891b86627bcce901c448974ad9cc99deb10:ef6a13c19a02436d627c10e032403bf1f90b6e84', 'src://7b4887ce51d9/internal/repos/builder.go@0df3b891b86627bcce901c448974ad9cc99deb10:a10b69cbf5d20057fbef3be5fafc42bade0fe9c0']
---
# initClone MUST persist the upstream InitFromRemote RESOLVED, never the one the caller requested

`Service.InitFromRemote` RETURNS the upstream branch it adopted — `(string, error)`, and `initFromEmptyRemote` returns it too. `Manager.initClone` must pass that returned value to `SetRemote`, not `spec.Origin.Branch`.

Why the signature exists: when the caller passes an empty branch, InitFromRemote resolves one itself — prefer an existing "main", else the remote's symbolic HEAD, else "main" — and creates the local branch and fetch refspecs from THAT. A caller that then persists its own `if branch == "" { branch = "main" }` fallback writes a remotes row disagreeing with the refspecs the clone just built. For a master-convention origin the row says "main", the refs say "master", and every later sync reads a nonexistent origin/main. This is the same class of bug that `rehydrateUpstreamMain` exists to prevent on the reboot path.

Two related rules on this path:

- `initClone` passes the default ontology in `initFiles`. InitFromRemote IGNORES seed files when the remote has branches (content comes from the clone) and writes them when it does not — so without them, cloning an EMPTY origin produces a repo with no ontology file, unlike every repo from `initLocal`.
- `repoBuilder.upstreamMain` is loaded from the stored remote row by `rehydrateUpstreamMain` and is EMPTY when the repo has no origin. `setupIndex` uses that emptiness as the has-origin test. Never default it to "main": doing so would add a nonexistent branch to the startup index sync for every origin-less repo.

WHAT THIS DOES NOT MEAN: the caller-supplied branch is not ignored. A non-empty `OriginSpec.Branch` is honoured and returned unchanged; the rule is about which value gets PERSISTED, which is always the returned one.
