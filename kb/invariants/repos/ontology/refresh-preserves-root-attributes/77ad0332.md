---
kind: pragmatic
type: policy
domain: [repos, fact, ontology, verify]
confidence: 0.9
sources: 1
entities: [refreshDivergence, RootAttributesEqual, Ontology.SubsetDivergence, attrsNotSubset, repoBuilder.loadOntology, TestLoadOntology_RootAttributesSurvivePresetRefresh, TestRefreshDivergence_NeverAddsOrChangesRootAttributes, internal/repos/builder.go]
motifs: [bypass-defeats-guarantee, consumer-defines-check]
refs: ['src://7b4887ce51d9/internal/repos/builder.go@74f2ed300fa0cb70cf675d8c2d5b728bd1c6c6e6:8cc1358b9d6b498f64de9451d2ae652944a12bd5', 'src://7b4887ce51d9/internal/fact/ontology.go@74f2ed300fa0cb70cf675d8c2d5b728bd1c6c6e6:a8e1caace56632a417bfb55235797844ff19ca38', 'src://7b4887ce51d9/internal/repos/builder_ontology_test.go@74f2ed300fa0cb70cf675d8c2d5b728bd1c6c6e6:2cec56019882982f77ae4c22622cd83e5e04a214', 'kb://3ec012f5b4d2/kb/decisions/fact/ontology/attributes-block-preset-refresh/17bba4fd.md', 'https://github.com/knomit/knomit/pull/312']
---
# The boot preset refresh may only PRESERVE root attributes: SubsetDivergence counts them and refreshDivergence also refuses a preset that would ADD, CHANGE or DROP one — otherwise a release bump erases verify_signatures fleet-wide in an own-signed commit

The boot refresh in `repoBuilder.loadOntology` writes the embedded preset over a stored preset-derived ontology when it is a subset and the bytes differ. That write is a commit SIGNED BY THIS INSTANCE. Under F09 a signed change to `verify_signatures`/`verify_signers` is a POLICY change, so an upgrade must never make one.

TWO GUARDS, both required:
1. `Ontology.SubsetDivergence` compares ROOT attributes exactly like node attributes (`attrsNotSubset`, absent-aware). A stored root attribute the preset lacks is DivergenceAttributes, so the refresh is skipped with reason "attributes". Before this, Serialize dropped the root block and SubsetDivergence never looked, so the next preset bump would have erased the setting on every instance (proposal test 23 = TestLoadOntology_RootAttributesSurvivePresetRefresh).
2. `refreshDivergence` (builder.go) adds `fact.RootAttributesEqual(stored, preset)`: a preset that CARRIES a root attribute the stored file lacks, or a different value, also blocks the refresh. SubsetDivergence alone would allow a preset to ADD a setting.

The guards overlap on the "stored has, preset lacks" case, so removing only one of them does not turn test 23 red; TestRootAttributes_AreDivergence (fact) and TestRefreshDivergence_NeverAddsOrChangesRootAttributes (repos) pin each guard separately.

An explicit `verify_signatures: off` compares as absent, so spelling out the default does not block upgrades.

DOES NOT MEAN: embedded presets may ship root attributes to turn verification on. They cannot: any preset root attribute is divergence for every repo that lacks it.
