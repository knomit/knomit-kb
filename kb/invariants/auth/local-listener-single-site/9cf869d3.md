---
kind: pragmatic
type: policy
domain: [auth, desktop, bridge, windows, architecture]
confidence: 0.9
sources: 1
entities: [auth.ListenLocal, auth.ConnContext, auth.ErrSocketInUse, auth.RequireLocalListener, auth.LocalVia, cmd/serve.go, openLocalListener, tools/desktop/boot.go, bootServer, localListenerFrom, server.closeSocket, lockfile.Write, config.Socket, TestBootServer_OpensLocalSocketWithPeerCreds, TestBootServer_SocketFailureWritesNoLockfile, TestBootServer_PipeFailureWritesNoLockfile]
motifs: [single-chokepoint, two-construction-paths]
refs: ['src://7b4887ce51d9/internal/auth/listen.go@e58f08a34a481fa682f798d1b62f84a54c34c058:4f4148a1d73c67d9b5e8db306c6e357fb73eafd0', 'src://7b4887ce51d9/internal/auth/listen_unix.go@e58f08a34a481fa682f798d1b62f84a54c34c058:60b747d4273654b76934c2802dd68f9da2abc9b2', 'src://7b4887ce51d9/internal/auth/listen_windows.go@e58f08a34a481fa682f798d1b62f84a54c34c058:76769918271bef4ac25a667f57c1d80481abfce5', 'src://7b4887ce51d9/cmd/serve.go@e58f08a34a481fa682f798d1b62f84a54c34c058:590901406eda4caad093b60a82271ba35301aa98', 'src://7b4887ce51d9/cmd/listen.go@e58f08a34a481fa682f798d1b62f84a54c34c058:3cdba2434135fd805577e629650317df84612c97', 'src://7b4887ce51d9/tools/desktop/boot.go@e58f08a34a481fa682f798d1b62f84a54c34c058:0effd841d0692278215abfa1cfc3ff8f1b7e34c4', 'src://7b4887ce51d9/tools/desktop/app.go@e58f08a34a481fa682f798d1b62f84a54c34c058:7df56d110428b459ea6c14ead244ba0a19165481', 'src://7b4887ce51d9/tools/desktop/boot_socket_unix_test.go@e58f08a34a481fa682f798d1b62f84a54c34c058:ef1259dd161e595031542e453dbd5d86fd60c581', 'src://7b4887ce51d9/tools/desktop/boot_pipe_windows_test.go@e58f08a34a481fa682f798d1b62f84a54c34c058:9fcb3279f63c3d3f524cac4e7679b70a5c4cd567', 'kb://3ec012f5b4d2/kb/gotchas/desktop/separate-server-boot-path/cd92bf55.md', 'kb://3ec012f5b4d2/kb/decisions/auth/socket-liveness-flock/b27c556d.md', 'kb://3ec012f5b4d2/kb/decisions/auth/socket-peer-creds-for-local-bridge/e6369a31.md', 'kb://3ec012f5b4d2/kb/decisions/auth/windows-local-listener-is-a-named-pipe/5b6e19d0.md', 'kb://3ec012f5b4d2/kb/decisions/auth/windows-pipe-namespace-is-the-liveness-lock/adadd5e1.md', 'kb://3ec012f5b4d2/kb/invariants/auth/require-needs-a-bound-listener/0f2335f4.md', 'https://github.com/knomit/knomit/issues/248', 'https://github.com/knomit/knomit/issues/245', 'https://github.com/knomit/knomit/pull/250', 'https://github.com/knomit/knomit/pull/251']
---
# auth.ListenLocal is the ONLY place the local authenticated listener is opened — a unix socket, or a named pipe on Windows — and BOTH server binaries must call it, set ConnContext: auth.ConnContext, call the cleanup, AND consult auth.RequireLocalListener; skipping any of the four ships anonymous bridges or a silent lockout

THE RULE: the local authenticated listener is opened in exactly ONE function, `auth.ListenLocal(path)` in internal/auth/listen.go. WHAT it is differs by platform and that difference is the only thing the per-platform halves decide:

- **unix** — a domain socket whose kernel-reported peer uid becomes `bridge:uid:<n>@socket`. Stale removal, listen, chmod 0600, the flock liveness lock and cleanup live in listen_unix.go.
- **windows** — a named pipe whose impersonated client SID becomes `bridge:sid:<SID>@pipe` (knomit#245). SDDL, the FILE_CREATE liveness refusal and cleanup live in listen_windows.go. There is NO lock file there; the pipe namespace is the lock.

It is the only non-test `net.Listen("unix", …)` and the only non-test `winio.ListenPipe` in the repo.

FOUR STEPS, ALL REQUIRED at every serving path. There are two server boot paths and they share only the handler (see the desktop boot-path gotcha): `cmd/serve.go` (`knomit serve`, via `openLocalListener` in cmd/listen.go) and `tools/desktop/boot.go` `bootServer` (the desktop app, the normal install). EACH must:

1. call `auth.ListenLocal(cfg.Socket)` and serve the returned listener on its `http.Server`;
2. set `ConnContext: auth.ConnContext` on that `http.Server` literal;
3. keep the returned cleanup for the listener's life and CALL it on shutdown;
4. call `auth.RequireLocalListener(require, ln, path, err)` straight after listening.

Step 1 without step 2 is the quiet failure: the listener exists and answers, the bridge connects happily, and every request still carries NO peer credential, so the principal is anonymous exactly as if there were no listener. `TestBootServer_OpensLocalSocketWithPeerCreds` asserts the peer arrives through the desktop's server, not merely that the listener answers — dropping the ConnContext line fails it.

Step 4 is the one added by knomit#245, and skipping it ships the SILENT LOCKOUT: with `[auth].require = true` there is no anonymous path, so a boot that took the benign ErrSocketInUse branch would answer every request 403 while looking healthy. app.checkLocalListener cannot see this — it asks what is CONFIGURED, not what was BOUND. See the require-needs-a-bound-listener invariant.

CALLER CONTRACT for the results of ListenLocal:
- `(ln, cleanup, nil)`: serve `ln`; keep `cleanup` and call it on shutdown (serve.go defers it; the desktop stores it as `server.closeSocket` and calls it in `shutdown()` after `http.Shutdown`). Both halves matter. Dropping `cleanup` lets the GC close the unix lock descriptor early. Not calling it keeps the lock after the listener is gone — and `http.Server.Shutdown` alone already unlinks the socket FILE, so a missing call is invisible to any "file is gone" check; what breaks is the successor, e.g. the desktop's `RestartApp` relaunch, which finds the lock held, gets `ErrSocketInUse` and serves TCP only (anonymous bridges) until a manual restart. On Windows the same call is what frees the pipe NAME for a successor.
- `ErrSocketInUse`: another live PROCESS holds the path. WARN and serve TCP only; NEVER fatal, NEVER remove — unless step 4 says otherwise. It is "another live process" and not "another knomit": on unix the .lock file is knomit's own so the owner almost certainly is knomit, but on Windows the pipe namespace is world-creatable and ERROR_ACCESS_DENIED is what the OS returns for ANY foreign owner.
- any other error: a real failure. Both callers fail the boot — serve.go by RETURNING it from `openLocalListener` (not log.Fatal, so the deferred cleanups and the crash marker run), the desktop by returning it from `bootServer`.
- `(nil, noop, nil)`: path "" only — skip; no platform branch in callers. NOTE: this is no longer the Windows case. Windows opens a real pipe, and a path outside the pipe namespace is an ERROR there, not a silent skip, because CreateFile would otherwise open an ordinary path as a FILE and the server would report a listener while accepting nothing.

ORDERING in the desktop: ListenLocal and the step-4 check both run BEFORE `lockfile.Write`, so a listener failure never leaves a discovery lockfile advertising a TCP port nothing serves (`TestBootServer_SocketFailureWritesNoLockfile` on unix, `TestBootServer_PipeFailureWritesNoLockfile` on Windows; both fail if the order is swapped).

HOW TO APPLY: adding a third way to serve knomit (another binary, a test harness that stands in for production, a new listener type) means doing ALL FOUR steps there too. Grep for `http.Server{` in any new boot code and check all four are present. Do not copy the listener steps inline — that is how phase 1 ended up with a socket the desktop never opened.

MISREADING TO AVOID: this is not "all listeners go through ListenLocal". The TCP listener (`netutil.Listen` in the desktop, `ListenAndServe` in serve.go), the runtime diagnostics port and the mTLS listener (`auth.ListenTLS`, its own http.Server with `auth.TLSConnContext`) are separate and stay separate; the rule covers the local AUTHENTICATED listener only.
