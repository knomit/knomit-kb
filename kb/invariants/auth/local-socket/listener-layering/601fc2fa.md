---
kind: pragmatic
type: policy
domain: [config, auth, bridge]
confidence: 0.9
sources: 1
entities: [config.socketFor, config.homeAndConfig, config.SocketPath, config.Load, Config.Socket, knomitapi.SocketPath, KNOMIT_SOCKET, KNOMIT_HOME, knomit.toml, findConfigFile, expandTilde, localListenerName]
motifs: [silent-drift-between-binaries, single-site-decision]
refs: ['src://7b4887ce51d9/internal/config/paths.go@aad7f8bef1953ecf406d83d9d5d9cfe0e5c257f7:9666282b411b7699e10040687ade6cfa7e54a977', 'src://7b4887ce51d9/internal/config/config.go@aad7f8bef1953ecf406d83d9d5d9cfe0e5c257f7:d4fb0a635283dbcbafb656b46428157f57819858', 'src://7b4887ce51d9/internal/config/socket_agreement_test.go@aad7f8bef1953ecf406d83d9d5d9cfe0e5c257f7:f9f462cad53b44108e3a8bbd6dcb3d0b0ef93fbb', 'src://7b4887ce51d9/tools/bridge/knomitapi/socketpath_test.go@aad7f8bef1953ecf406d83d9d5d9cfe0e5c257f7:8556159830bc13926c0b4e250fc8c7d76be950b3', 'src://7b4887ce51d9/tools/bridge/knomitapi/dial.go@aad7f8bef1953ecf406d83d9d5d9cfe0e5c257f7:d3e923cb4f896418cb8150d4a6dc80b2a7dda6fd', 'https://github.com/knomit/knomit/issues/271', 'https://github.com/knomit/knomit/pull/272', 'kb://3ec012f5b4d2/kb/decisions/auth/local-socket/long-home-fallback/15623208.md', 'kb://3ec012f5b4d2/kb/invariants/auth/local-listener-single-site/9cf869d3.md', 'kb://3ec012f5b4d2/kb/gotchas/config/socket-path-not-load/963e6a17.md']
---
# The local listener path is decided ONLY by config.socketFor(home, fromTOML, fromEnv) — KNOMIT_SOCKET > knomit.toml `socket` > localListenerName(tilde-expanded home) — and BOTH config.Load (server) and config.SocketPath (bridge) start from config.homeAndConfig; the bridge never resolves any layer itself

knomit#271 / PR #272. Two unexported functions in internal/config/paths.go together decide the local authenticated listener (unix socket, or Windows named pipe):

1. `homeAndConfig() (home, configPath string, err error)`: ResolveHome, THEN expandTilde, THEN findConfigFile on the EXPANDED home. It is the one place that order lives. Load no longer tilde-expands Home in its later path loop; Home is expanded exactly once, here.
2. `socketFor(home, fromTOML, fromEnv string) string`, PURE: fromEnv (the caller passes os.Getenv("KNOMIT_SOCKET")) if non-empty, else fromTOML, else `localListenerName(home)`.

BOTH resolvers use both:
- `config.Load` (what `knomit serve` and the desktop pass to auth.ListenLocal): homeAndConfig → decode knomit.toml into cfg → … → `cfg.Socket = socketFor(cfg.Home, cfg.Socket, os.Getenv("KNOMIT_SOCKET"))`. Its env-overlay block has a comment saying KNOMIT_SOCKET is applied in socketFor. Do not add an envOr for it there.
- `config.SocketPath` (what `tools/bridge/knomitapi.SocketPath` delegates to): homeAndConfig → decode the file into a zero `Config` and read `.Socket` → socketFor.

Where the listener is OPENED is a separate invariant (auth.ListenLocal, local-listener-single-site). This one is about which PATH both sides compute.

SERVER BEHAVIOUR CHANGE, DELIBERATE (found in the #272 review): before this, Load called findConfigFile(cfg.Home) BEFORE tilde-expanding Home. A data root given as `KNOMIT_HOME=~/kh` therefore searched a literal "~/kh" directory and silently ignored `$HOME/kh/knomit.toml`, for EVERY setting and not only the socket. Since #272 the server reads that file. The first version of #272 had the bridge expand first while Load did not, so with a "~/" root plus a TOML socket the two sides disagreed. homeAndConfig exists so that order cannot diverge again.

CONSEQUENCES:
- A new layer (another env var, a flag) goes into socketFor's inputs at BOTH call sites, never into one resolver. Changing one side only reproduces #271: the bridge dials a path the server never opened, finds nothing, falls back to TCP, and silently loses the verified identity. No error is raised anywhere.
- Any change to how the data root or its knomit.toml is found goes in homeAndConfig, not in Load or SocketPath.
- `knomitapi.SocketPath` stays a thin delegate. Any bridge-side reading of KNOMIT_SOCKET, TOML or HOME is a violation even if it happens to compute the same answer today. Its WARN on failure fires once per process (sync.Once), but the path itself is resolved on every call and never cached (ambient-state-at-package-init).
- A knomit.toml that SocketPath cannot decode is an error (the bridge logs WARN and uses TCP), not a fall-through to the default, because Load refuses the same file.

ENFORCED BY: internal/config/socket_agreement_test.go. It is platform-neutral, and every KNOMIT_* variable is cleared per test. Each case asserts Load().Socket == SocketPath() == expected, for: KNOMIT_SOCKET; TOML socket; env beats TOML; `~/` in KNOMIT_HOME; `~/` in KNOMIT_HOME together with a TOML socket in the expanded root. A malformed TOML must make SocketPath return an error. tools/bridge/knomitapi/socketpath_test.go pins the delegation, and the existing long-home agreement test still stands.

WHAT THIS DOES NOT MEAN:
- Not that the chosen socket value is tilde-expanded. `socket = "~/x.sock"` stays literal on BOTH sides (Socket is not in Load's tilde list). The two sides agree; the missing expansion is a pre-existing bug left as a follow-up.
- Not full parity for a knomit.toml placed BESIDE ONE EXECUTABLE, or for a RELATIVE socket path. See the gotcha on the beside-the-executable lookup.
