---
kind: pragmatic
type: policy
domain: [auth, web, mcp, security]
confidence: 0.9
sources: 1
entities: [auth.Principal, auth.Allowed, auth.Grants, auth.Set, auth.ParseSet, AuthMiddleware, Require, writeGate, gatePermission, permissionFilter, loopbackGrants, Server.grants]
motifs: [single-chokepoint-authorization, default-deny-on-error]
refs: ['src://7b4887ce51d9/internal/auth/permission.go@cec7c5c10b4a752dadbb34a45cb749427923ba8d:36711045c2d8dc55b325b5fa435f7889f18f077c', 'src://7b4887ce51d9/internal/auth/principal.go@cec7c5c10b4a752dadbb34a45cb749427923ba8d:f8dbcdb73210aaaf45c5534116b5193ed9e64433', 'src://7b4887ce51d9/internal/web/auth_middleware.go@cec7c5c10b4a752dadbb34a45cb749427923ba8d:8dc48207941b3ccbcadccb284305e61238fcf387', 'src://7b4887ce51d9/internal/mcp/permission_gate.go@cec7c5c10b4a752dadbb34a45cb749427923ba8d:8ab40e3224ae69f9630d495c6afb8f997850d4f1', 'https://github.com/knomit/knomit/pull/243']
---
# Every transport becomes an auth.Principal at the EDGE and auth.Allowed is the only permission check — default deny on a zero principal, an unknown principal AND a store error alike

A request's identity is decided ONCE, at the edge, and never re-derived downstream. `internal/web.AuthMiddleware` turns a transport into an `auth.Principal` and puts it on the context; `internal/auth.Allowed(ctx, grants, principal, perm)` is the ONLY function that answers whether it may do a thing. Two enforcement points call it — `writeGate` on mutating HTTP routes and `gatePermission` on MCP write tools — and both consult the SAME `auth.Grants` value, built once per `Handler()` by `Server.grants()`, so they cannot come to disagree about what anonymous holds.

DEFAULT DENY, and the compound condition matters: `Allowed` returns false for a ZERO principal, for a principal with no grants row, for a nil Grants, AND for a grants STORE ERROR. The last one is the one that gets designed away — a database that is down must refuse, not fall open, and the error is deliberately not returned because no caller could do anything with it but refuse anyway.

CONSEQUENCE a consumer must act on: a new mutating surface is NOT protected by existing code. It is protected only if it sits behind one of the two enforcement points. `writeGate` covers the API router and, separately, the `/git` mount on the outer router; anything mounted elsewhere in `Handler()` is ungated and adding it there is a silent hole.

THE MISREADING TO AVOID: this does not say every permission is enforced. Six permissions exist as constants and as grants rows — read, write, push:own, merge:main, operator, admin — and in phase 1 only `write` has ANY enforcement point. Reading the presence of `merge:main` in the vocabulary as protection for merges would be wrong; F11/F12/phase 4 add those checks. Likewise `Read` is defined but nothing gates on it, because a request that reaches the server has already passed the transport boundary.

`ParseSet` rejects an unknown permission name as an ERROR rather than dropping it, so a typo in `[auth].loopback_default` fails at wiring time instead of silently granting less than the operator wrote — or, after a future rename, more.
