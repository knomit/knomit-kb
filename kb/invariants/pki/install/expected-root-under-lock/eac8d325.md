---
kind: pragmatic
type: policy
domain: [pki, security, desktop, cli, concurrency]
confidence: 0.9
sources: 1
entities: [pki.InstallBundle, pki.InstallOptions, ExpectInstalledRoot, pki.RootDiffersError, pki.LockPath, pki.LockFile, lockInstall, pki.Status, pki.PreviewBundle, NativeService.InstallBundle, installBundle, confirmRoot, TestInstallBundle_RootChangedSinceReadIsRefused, TestInstallBundle_FirstInstallIsCheckedAgainstNoRoot, TestInstallBundle_WaitsForTheInstallLock]
motifs: [check-then-act-race, out-of-band-confirmation]
refs: ['kb://3ec012f5b4d2/kb/decisions/pki/install/expected-root-under-lock/b86086b2.md', 'kb://3ec012f5b4d2/kb/decisions/pki/install/placement/92131992.md', 'kb://3ec012f5b4d2/kb/invariants/desktop/fleet/install-surface/a46bbb69.md', 'src://7b4887ce51d9/internal/pki/install.go@4302ea88607748889a35d0e0c76e6622df4067fe:e71f87432a87c2ebbef993f71f962fee8617508b#L142-L240', 'src://7b4887ce51d9/internal/pki/lock.go@4302ea88607748889a35d0e0c76e6622df4067fe:fca85063fe05677a6cee540873b5d2fa850d89e1', 'src://7b4887ce51d9/internal/pki/lock_unix.go@4302ea88607748889a35d0e0c76e6622df4067fe:065d9bea604f6ac273d10ef7d5652350167c5d17', 'src://7b4887ce51d9/internal/pki/lock_windows.go@4302ea88607748889a35d0e0c76e6622df4067fe:7b061707538dc5768bb74348b30e56a495fb8303', 'src://7b4887ce51d9/internal/pki/install_test.go@4302ea88607748889a35d0e0c76e6622df4067fe:450cea24ea00a5a2cfda71a8e1662ae47d891894#L378-L485', 'src://7b4887ce51d9/tools/desktop/identity.go@4302ea88607748889a35d0e0c76e6622df4067fe:5cef9ffd4a099771d8cc604d677eb2a5d44a4186#L200-L294', 'src://7b4887ce51d9/cmd/identity.go@4302ea88607748889a35d0e0c76e6622df4067fe:b0bc627bf3313a0a86273726d8dc64511bb13938#L257-L360', 'https://github.com/knomit/knomit/issues/299']
---
# An identity install is decided against the root the CALLER SAW, under a cross-process lock: pki.InstallBundle installs only if the installed root ("" = none) still equals InstallOptions.ExpectInstalledRoot when read under <pki>.lock, and refuses otherwise with a RootDiffersError naming the root it found — never re-reading and proceeding

RULE. Every production caller of `pki.InstallBundle` (the desktop Settings binding and `cmd/identity.go`, pinned by test/archtest) passes `InstallOptions{ExpectInstalledRoot}` = the RootID fingerprint of the root it READ as installed and — when that root is new to this instance — SHOWED the user and had confirmed; "" means it saw no root installed. Under a cross-process exclusive lock on `<pki>.lock` (flock / LockFileEx, `pki.LockFile`), InstallBundle reads root.crt and:
- installed fingerprint != ExpectInstalledRoot → refuse with `*RootDiffersError{Installed: <what it found, empty when none>, Bundle}`; nothing written;
- root.crt present but unreadable or not Ed25519 → refuse, ALWAYS (no expected value can vouch for bytes nobody could have seen); removing the file is the way out;
- otherwise the CRL watermark check, then the writes, still under the lock.

The lock is held from that read through the writes and the watermark record, so the root compared is the root replaced. There is no replaceRoot flag: expecting the installed root while installing a bundle from another root IS the move to another fleet.

CONSEQUENCES for a caller:
- Read the installed root with `pki.Status` (or LoadRootCert + RootID) and pass exactly that; never pass the bundle's own root, and never re-read and retry on RootDiffersError — surface it (desktop: confirmation_stale with the new installed fingerprint; CLI: "the installed fleet root changed while this command ran").
- Collecting consent is YOURS, not pki's: pki will install a first bundle for ExpectInstalledRoot "" and a foreign root for ExpectInstalledRoot = current without asking anyone. The desktop refuses an unseen root as root_unconfirmed / root_differs and installs only on a second call carrying the shown pair; the CLI prints the root and principal and needs --root <fp>, --yes, or a y/N answer on a terminal. A renewal (bundle root == installed root) needs no consent.
- Run `pki.PreviewBundle(keyPath, raw)` before asking: it runs the bundle-and-key checks, so the user is never asked to trust the root of a bundle that could never install.
- The lock file is a SIBLING of <pki>, never inside it, so a refused install still leaves <pki> exactly as it was (absent on a fresh home); only <pki>'s parent is created, 0700. The lock file is never deleted: unlinking a lock file lets a waiter hold a lock on an orphaned inode while a newcomer locks a fresh one.
- The wait for the lock is bounded (installLockWait); a timeout names the lock path.

MISREADINGS:
- "The desktop's installMu serialises installs." Only within one process; the CLI is another process. The pki lock is what serialises them.
- "A first install has nothing to compare against." It compares against "none installed" (so a root installed underneath by another process is refused), and the caller compares the bundle root with the operator's out-of-band fingerprint.
- "Atomic renames make concurrent installs safe." They keep the FILES consistent; they do not keep the DECISION (which root was confirmed) true. That is the lock's job.

Proven by internal/pki/install_test.go: TestInstallBundle_RootChangedSinceReadIsRefused (A installed and read; C installed by "another process"; B expecting A → RootDiffersError naming C, C intact — deterministic, no goroutines), TestInstallBundle_FirstInstallIsCheckedAgainstNoRoot (parent created, <pki> absent after refusal, lock beside it), TestInstallBundle_WaitsForTheInstallLock, TestInstallBundle_UnreadableInstalledRootFailsClosed.
