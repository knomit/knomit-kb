---
kind: pragmatic
type: policy
domain: [pki, auth, identity, security]
confidence: 0.9
sources: 1
entities: [pki.Fingerprint, pki.Short, pki.LoadSigner, ensureKeyPair, fingerprint, agentBranch, ssh.NewPublicKey, auth.InstancePrincipal, ErrSANMismatch]
motifs: [one-definition-many-comparators, same-name-different-hash]
refs: ['src://7b4887ce51d9/internal/pki/key.go@91c89e078ec9c9537aee8ef97a4f6d376579a47f:752474cad79a97f9e6827fd9f9887d9b09127ab0', 'src://7b4887ce51d9/internal/app/identity.go@91c89e078ec9c9537aee8ef97a4f6d376579a47f:0b55d36ae486ed9d44002928afd8cd35dc312f45', 'src://7b4887ce51d9/internal/pki/key_test.go@91c89e078ec9c9537aee8ef97a4f6d376579a47f:ce8a831262fea545c3154267ce639ef460c11f4b', 'src://7b4887ce51d9/internal/app/identity_pki_test.go@91c89e078ec9c9537aee8ef97a4f6d376579a47f:b171394a0a1dffb8821a48e2081ffe7b8c245385', 'kb://3ec012f5b4d2/kb/decisions/identity/agent-branch-from-key-fingerprint/3ee0414b.md']
---
# An instance key's fingerprint is hex(sha256(SSH WIRE encoding)) — never SPKI DER, never the raw 32 bytes — and principal IDs carry all 64 hex while the branch name and certificate SAN carry only the first 8

THE ONE DEFINITION: `pki.Fingerprint(pub) = hex(sha256(ssh.NewPublicKey(pub).Marshal()))`, 64 hex characters. It is the same hash `internal/app/identity.go` `fingerprint()` computes over `signer.PublicKey().Marshal()` and truncates to 8 for the agent branch name `agent/<host>-<fp8>`. Three things compare against it and all three must use this computation: the agent branch name, the certificate SAN `knomit://agent/<host>-<fp8>`, and the principal ID of an instance (`instance:<64 hex>@cert`, the grants key).

FORBIDDEN alternatives, both of which look reasonable: sha256 over the X.509 SubjectPublicKeyInfo DER (what most PKI tooling calls "the key fingerprint"), and sha256 over the raw 32 Ed25519 bytes. Each yields a different value. Using either for anything compared to a branch name or a principal does not fail loudly: every enrolled peer is refused with ErrSANMismatch, or a grants row silently names no one.

FULL vs SHORT: `pki.Short` (8 hex, 32 bits) is a display and CROSS-CHECK value only — the SAN's fp8 must equal Short(Fingerprint(cert key)) or the handshake is refused. It is never an identity, because 32 bits can be ground. Anything that authorizes (grants rows, Principal.ID) uses all 64.

ENFORCED BY TESTS, each verified to fail when Fingerprint was sabotaged to sha256(raw key): `TestFingerprint_IsSSHWireNotSPKIOrRaw` (pins the value AND asserts inequality with both forbidden forms), `TestShort_EqualsTheBranchNameFingerprint` (reproduces identity.go inline), and `TestEnsureKeyPair_FingerprintIsPrefixOfPKIFingerprint` in internal/app (calls the real ensureKeyPair). If you change identity.go's fingerprint, the last one fails — that is the point; changing it also renames every agent branch.

MISREADING TO AVOID: the older decision fact says the branch fingerprint is 'SHA256(public_key_bytes)'. 'public key bytes' there means the SSH wire encoding (length-prefixed 'ssh-ed25519' + length-prefixed key), not the 32 raw bytes. Also: LoadSigner turns the OpenSSH-format key into an in-memory crypto.Signer; it never writes the key in another format, so ~/.knomit/id_ed25519 remains the only on-disk copy.
