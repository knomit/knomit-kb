---
kind: pragmatic
type: policy
domain: [web, routing, spa]
confidence: 0.95
sources: 1
entities: [Server.Handler, newSPAHandler, newAssetHandler, staticHandlerFor, embeddedStaticFS, Server.staticFS, serveIndex, identityForRangeRequests, compressor, assetsPrefix, APIBase, internal/web/server.go, internal/web/static.go]
refs: ['src://7b4887ce51d9/internal/web/server.go@aa37eb350e74282d02100adaed4c25a5037e4cd2:f9f93dc9f749ca278e79e13599fb5b7bb0009738#L110-L150', 'src://7b4887ce51d9/internal/web/static.go@aa37eb350e74282d02100adaed4c25a5037e4cd2:28071b2077d04db332d635f32121907e2fd8e79b', 'src://7b4887ce51d9/internal/web/embed.go@aa37eb350e74282d02100adaed4c25a5037e4cd2:a351ef3308708a7709fb21e864a35eeb2bc4c34b', 'src://7b4887ce51d9/internal/web/embed_noembed.go@aa37eb350e74282d02100adaed4c25a5037e4cd2:7b370b62fba73edf65a11b95735be93f9786390e', 'kb://3ec012f5b4d2/kb/decisions/web/static/compress-and-cache/d98cfbc9.md', 'kb://3ec012f5b4d2/kb/gotchas/web/spa/fileserver-index-redirect-loop/68987070.md', 'https://github.com/knomit/knomit/pull/223']
---
# Handler mount order: /git → /docs → /api/v1 → /assets/* → /* SPA fallback

Server.Handler() mounts paths in this strict order (chi matches in registration order): (1) /git — optional smart git HTTP if s.GitHandler != nil; (2) /docs — Swagger UI for the OpenAPI spec; (3) APIBase (= /api/v1) — the HAL API router mounted via r.Mount, which carries its own compressor(); (4) and (5) inside ONE r.Group that applies identityForRangeRequests then compressor(): `/assets/*` (assetsPrefix+"*") via newAssetHandler(fsys, staticHandler), and `/*` via newSPAHandler(fsys, staticHandler, tags) which serves index.html for any unmatched path so client-side routes survive a page refresh. Order matters: /api/v1/* and /assets/* must be matched BEFORE the catch-all /* or it intercepts everything. Anti-pattern: registering the SPA fallback before the API routes — every API call would return index.html.

**Shape since PR #223 (dev aa37eb35).** There is no `StaticHandler()` any more. The build-tagged files (`embed.go` / `embed_noembed.go`) expose only `embeddedStaticFS() fs.FS` (nil under `noembed`); the untagged `static.go` owns `staticHandlerFor(fsys)`, `newAssetHandler`, `newSPAHandler(fsys, static, *etagger)` and `serveIndex`, so both build variants share one code path and tests inject a `fstest.MapFS` through the unexported `Server.staticFS` seam. The static group sits in a Group, NOT on the outer router, so the API mount is not double-compressed. The SPA fallback serves index.html through `http.ServeContent` — never by re-dispatching a rewritten path to `http.FileServer`, which 301s any path ending in index.html to "./" (see kb/gotchas/web/spa/fileserver-index-redirect-loop/68987070.md). Cache policy, ETag and Range handling for this group are in kb/decisions/web/static/compress-and-cache/d98cfbc9.md.
