---
kind: pragmatic
type: policy
domain: [synthesize, fact, kind]
confidence: 0.95
sources: 0
entities: [reviewStrategy.AcceptSeed, reviewStrategy.SeedQuery, dirtyFacts, fact.Epistemic, fact.Pragmatic, fact.Signal, mergedFact, distillFact, internal/synthesize/review_strategy.go]
motifs: [redundant-check-unequally-reachable]
refs: ['src://7b4887ce51d9/internal/synthesize/review_strategy.go@a7bed6733bd534cbc30f181e12c3d9612559432d:91cb544343d02d6424aa082b5cf524b7614bc359', 'src://7b4887ce51d9/internal/synthesize/pipeline.go@a7bed6733bd534cbc30f181e12c3d9612559432d:e19a43a79b10203d52be9ca4e5e3a00f848b48fe', 'src://7b4887ce51d9/internal/synthesize/decision.go@a7bed6733bd534cbc30f181e12c3d9612559432d:d52e4cf925eb665863c80d8c0b69e0f702823ce7', 'kb://3ec012f5b4d2/kb/gotchas/synthesize/seed-filter/exclusion-differs-by-scan-path/0bc8c031.md', 'kb://3ec012f5b4d2/kb/decisions/fact/pragmatic-types/signal/e48a7a32.md']
---
# The seed kind filter is reviewStrategy.AcceptSeed (dirtyFacts is its CALLER); seeds are kind=epistemic ONLY and pragmatic facts never enter synthesis

Seeds are restricted to kind=epistemic ONLY — pragmatic facts (policy, heuristic, signal) are EXCLUDED from the synthesis pipeline.

WHERE THE FILTER LIVES. It is `reviewStrategy.AcceptSeed(f) bool { return f.Kind == fact.Epistemic }` in internal/synthesize/review_strategy.go. `dirtyFacts` (pipeline.go) is the CALLER that applies it on both scan paths, not the filter itself — this fact previously named dirtyFacts and pointed at review.go, and anyone grepping review.go for the rule now finds nothing. dirtyFacts normalises an empty Kind to DefaultKind before the check, so a fact file with no `kind:` line is treated as epistemic and still seeds.

The rule is stated twice, and the two statements are NOT equally reachable:

1. `SeedQuery()` pushes `IncludeKinds: [epistemic]` into SQL. On the FULL-SCAN path (no watermark) this is what actually excludes a pragmatic fact — it never comes back from the index, so AcceptSeed is never asked.
2. `AcceptSeed` is reached only on the INCREMENTAL path, where DiffFiles + ParseFact hands it every changed fact.

AcceptSeed is the authoritative STATEMENT of the rule and the only guard on the incremental path; the SQL clause is there for efficiency but is load-bearing on the full scan. See [[exclusion-differs-by-scan-path]] — it matters when writing tests, because a full-scan assertion pins the SQL clause and proves nothing about AcceptSeed.

WHY the rule exists: the decision-application code (mergedFact, distillFact in decision.go/types.go) does NOT carry Kind through. Letting a pragmatic fact in would have it silently rewritten as kind=epistemic on commit and the original deleted — corrupting policies, heuristics and signals. This is a correctness filter, not a preference.

What this does NOT mean: do not "fix" mergedFact/distillFact to carry kind as a side-quest. If they are ever extended to do so, this filter may be removed — but not one commit before.

Signal (F01) inherits this exclusion whole, by kind. IN THE SEED FILTER no type-level exclusion was added for it and none should be — kind is the right granularity here, because every pragmatic type needs excluding for the same reason.

Do not generalise that sentence past this filter. The same-subject gate in knomit_learn is a DIFFERENT mechanism on the write path, and it does skip signal BY TYPE (learn_same_subject.go:145) — correctly, because there the granularity genuinely differs: policy and heuristic must still be able to collide with an incoming fact, and only signal has the task-id-as-entity shape that makes that gate meaningless. See [[same-subject-skips-signal-by-type]]. "Pragmatic facts are already excluded" is true of the seed pool and false of the write path.
