---
kind: pragmatic
type: policy
domain: [mcp, explain, refs]
confidence: 0.95
sources: 0
entities: [classifyRefs, classifiedRefs, explainFactEntry]
refs: ['src://knomit/internal/mcp/explain.go@307b67d']
---
# knomit_explain output classifies refs into local (.md) and external; empty slices serialize as []

knomit_explain output classifies each fact's refs into 'local' (ends in .md) and 'external' (everything else: http/https URLs, file://, etc.) via classifyRefs(refs []string) classifiedRefs in internal/mcp/explain.go. The classifiedRefs struct is {Local []string, External []string}. Empty slices are EXPLICITLY initialized to []string{} (not nil) so JSON serializes as `[]` not `null` — matches the HAL hard rule that arrays serialize as []. BFS only follows local refs (external URLs are returned to the agent for it to handle out-of-band). Anti-pattern: adding a new explain field that returns nil slices — always coerce to empty slice to keep response shape stable for clients.
