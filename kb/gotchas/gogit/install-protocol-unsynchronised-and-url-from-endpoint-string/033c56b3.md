---
type: observation
domain: [gogit, pki, repos, remote]
confidence: 0.9
sources: 1
entities: [client.InstallProtocol, client.Protocols, transport.Endpoint.String, plumbing.UnexpectedError, plumbing.PermanentError, pki.GitScheme, pki.RewriteEndpoint, pki.ClassifyPeerError, pki.InstallGitTransport, configureTransport]
motifs: [wrapper-hides-cause, process-global-registry]
refs: ['src://7b4887ce51d9/internal/pki/gittransport.go@38ccec1816070fe8207c0e831652753e8b5fc6f8:33b720c01a3fa63becb22d8c9e9c4540930d22aa', 'src://7b4887ce51d9/internal/pki/gittransport_test.go@38ccec1816070fe8207c0e831652753e8b5fc6f8:8fcf11cc985a288b67ec1576841978c67e23597d', 'https://github.com/go-git/go-git/blob/v5.19.2/plumbing/transport/http/common.go', 'https://github.com/go-git/go-git/blob/v5.19.2/plumbing/error.go', 'https://github.com/go-git/go-git/blob/v5.19.2/plumbing/transport/client/client.go', 'kb://3ec012f5b4d2/kb/gotchas/pki/openssh-key-is-not-x509keypair-input/1b6e1dda.md']
---
# A custom go-git scheme needs a wrapper that rewrites Protocol to https on a COPY, writes the unsynchronised protocol map once, and must re-expose errors that go-git's UnexpectedError hides — go-git v5.19.2

Structural facts about go-git v5.19.2 that shape knomit's knomit+https transport (internal/pki/gittransport.go). Line numbers are dated support, not the claim.

ONE — REGISTERING IS NOT ENOUGH. `client.Protocols` is a plain map keyed by `Endpoint.Protocol`, and `client.InstallProtocol("knomit+https", t)` adds a key without touching `"https"`. But go-git's http transport builds every request URL from `endpoint.String()` (plumbing/transport/http/common.go advertisedReferences, upload_pack.go UploadPack), which writes the Protocol verbatim, so registering `githttp.NewClient(c)` directly fails in net/http with 'unsupported protocol scheme'. The redirect guard (`ModifyEndpointIfRedirect`) also compares against `endpoint.Protocol` and MUTATES the session's endpoint. So knomit registers a wrapper whose sessions pass `pki.RewriteEndpoint(ep)` — a COPY with Protocol "https" — to the inner http transport. The caller's endpoint is never changed.

TWO — THE MAP IS UNSYNCHRONISED. Write it exactly once, before any clone or sync goroutine (app.New, before Manager.Start). `pki.InstallGitTransport` writes the map on its FIRST call only. The registered wrapper holds no state: each session resolves the mTLS client through an atomic pointer to a lazy source, which returns ErrNotEnrolled until a certificate exists and rebuilds when the pki files change. A later call with the same dir and keyPath is a no-op. A later call with a DIFFERENT dir or keyPath swaps the source (closing the old one's idle connections, logging one line naming both dirs), so the next session presents the new identity and the map is never written again. Tests pin all three: the swap, the single map write (a sentinel entry survives the second call), and the no-op.

THREE — go-git HIDES ERROR CAUSES. `plumbing.UnexpectedError` and `plumbing.PermanentError` have NO Unwrap method. The upload-pack POST (`doRequest`) wraps the net/http error in UnexpectedError, so `errors.Is(err, pki.ErrRevoked)` is false above go-git unless something peels it. The info/refs GET returns net/http's *url.Error unwrapped, and that one DOES unwrap. Measured by sabotage: with classification disabled, the other-root test (fails on info/refs) stayed green while the UploadPack test went red. `pki.ClassifyPeerError` peels the two go-git types and re-exposes the cause. It maps a remote TLS alert (`*net.OpError` with Op "remote error") to ErrRefusedByPeer and to nothing more specific, because an alert does not say why. It leaves errors with no go-git wrapper unchanged, so go-git's sentinel `==` checks still hold.

FOUR — ENDPOINT TLS FIELDS EDIT THE CLIENT. If an Endpoint carries ClientCert/ClientKey/CaBundle/InsecureSkipTLS or a Proxy, `newSession` clones the http.Transport and `configureTransport` edits its tls.Config (CaBundle appends to the SYSTEM pool). The knomit+https wrapper therefore REFUSES those fields, any go-git AuthMethod and URL userinfo, before dialling.

FIVE — THE SCHEME IS LOWERCASED. go-git parses with url.Parse, so KNOMIT+HTTPS:// arrives as Protocol "knomit+https". Checks on the raw URL string elsewhere (validateURLAuth, the wizard, resolveAuthWithOrigin) must be case-insensitive.

MISREADING TO AVOID: this is not 'go-git cannot do mTLS'. It can (ClientCert/CABundle), but those options use tls.X509KeyPair on the key, the system pool and a hostname check, and all three are wrong for knomit peers. See kb/gotchas/pki/openssh-key-is-not-x509keypair-input. Nor is it 'install once, identity fixed forever': the go-git registration is fixed, the identity behind it is swappable.
