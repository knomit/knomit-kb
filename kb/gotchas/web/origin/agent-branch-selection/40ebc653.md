---
type: observation
domain: []
confidence: 0.9
sources: 0
entities: [web/src/api.ts, getAgentBranch, internal/web/handlers_repos_hal.go, internal/store/branch.go, DefaultBranch, internal/web/handlers_facts_collection.go]
refs: ['src://knomit/web/src/api.ts@30f759e2e4e11507dfeeccaff8caeac8fc282dbb', 'src://knomit/internal/web/handlers_repos_hal.go@30f759e2e4e11507dfeeccaff8caeac8fc282dbb', 'src://knomit/internal/store/branch.go@30f759e2e4e11507dfeeccaff8caeac8fc282dbb']
---
# UI must default to server-authoritative agent_branch, not the first agent/* branch

Symptom: after connecting repo 'core' to a shared remote, the Library showed ZERO facts even though 836 were correctly indexed. Cause chain: the remote's HEAD pointed at a FOREIGN agent branch (agent/Alexs-MacBook-Air...), so CloneFrom adopted it as a local head and DefaultBranch() returned it (stored as the consensus -> push-only warning). The repo then had two agent/* git heads; only THIS machine's branch (agent/mindev.local-..., via MatchedAgent/agentBranch) was rebuilt/indexed at commit. The frontend getAgentBranch picked branches.find(b => b.name.startsWith('agent/')) — the first alphabetically — landing on the foreign branch with 0 indexed facts.

Key facts: (1) ListBranches/the /branches endpoint lists git heads, including foreign + unindexed branches. (2) branch names are colon-encoded in HAL URLs (agent:mindev... not agent/mindev...). (3) GET /api/v1/repos/{repo} returns the authoritative agent_branch (ri.AgentBranch()). Fix: getAgentBranch now prefers RepoDetails.agent_branch, falling back to the heuristic only when absent. No data loss — purely UI branch selection. Deeper trigger left unaddressed (user accepted the push-only consensus): a remote whose HEAD is an agent branch rather than main.
