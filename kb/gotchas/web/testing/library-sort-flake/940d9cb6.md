---
type: observation
domain: [web, testing, flake]
confidence: 0.9
sources: 1
entities: [Library.sort.test.tsx, web/src/Library.tsx]
motifs: [postponed-failure]
refs: ['https://github.com/knomit/knomit/pull/177']
---
# Library.sort 'ArrowDown in Recent mode navigates between facts' is a PRE-EXISTING flake — fails ~1 in 4 full runs, passes in isolation; do not attribute it to new work

During the motif UI branch (PR #177) this test failed intermittently in full-suite runs (~1 in 3–4) while always passing in isolation. Confirmed pre-existing by running the suite at the base commit in a clean worktree BEFORE assuming innocence — the flake reproduces there. Left unfixed as out of scope.

How to apply: when this test is the sole failure in a full run, retry once before investigating; do not count it as a regression against unrelated branches. The proper fix (making the test deterministic) is still owed — delete this fact when that lands.
