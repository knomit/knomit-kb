---
kind: pragmatic
type: policy
domain: [web, http, performance, go]
confidence: 0.95
sources: 1
entities: [middleware.Compress, defaultCompressibleContentTypes, application/hal+json, application/problem+json, hal.WriteHAL, hal.WriteProblem, internal/web/router.go, go-chi/chi/v5]
motifs: [allowlist-excludes-actual-type, mitigation-silently-inert]
refs: ['src://7b4887ce51d9/internal/web/router.go@3b6f80f564f1b7371e35ed152c5b7741bf029ccd:c17d3024a3db5f83d5a073d121938fd57ac9c739#L40', 'src://7b4887ce51d9/internal/web/hal/writer.go@3b6f80f564f1b7371e35ed152c5b7741bf029ccd:11fcc9191aef67697df6013891e993153e74b9fc', 'https://github.com/go-chi/chi/blob/v5.2.5/middleware/compress.go', 'kb://3ec012f5b4d2/kb/gotchas/web/sse/proxy-compression-stalls-streams/a762359e.md', 'kb://3ec012f5b4d2/kb/conventions/web/hal-response-hard-rules/b7f5c40d.md']
---
# chi's middleware.Compress with no types compresses ONLY its built-in list, which has application/json but NOT application/hal+json or application/problem+json — so knomit's API was never compressed despite the 'mandatory' comment; passing any types REPLACES the defaults, so the allowlist must be complete and must never include text/event-stream

`middleware.Compress(level)` with no content types uses chi's unexported `defaultCompressibleContentTypes`: text/html, text/css, text/plain, text/javascript, application/javascript, application/x-javascript, application/json, application/atom+xml, application/rss+xml, image/svg+xml. Matching is on the response's Content-Type. knomit writes every API body as `application/hal+json` (`hal.WriteHAL`) or `application/problem+json` (`hal.WriteProblem`), neither of which is in that list, so the middleware installed at `internal/web/router.go` passed every response through uncompressed. Verified 2026-09-18: `curl -H 'Accept-Encoding: gzip'` on a 56 KB motifs response returned no Content-Encoding, and the browser's encodedBodySize equalled decodedBodySize for every API entry.

**Consequences.** (1) A comment or convention saying 'compression is on' is not evidence; check `Content-Encoding` on a real hal+json response. (2) When you pass types to `Compress(level, types...)` chi uses ONLY those — the defaults are gone — so the list must carry html/css/js/json as well as the hal and problem types. (3) NEVER add `text/event-stream` or a `text/*` wildcard: chi's matcher accepts wildcards, and compressing SSE stalls every stream silently (see the proxy-compression gotcha). (4) Already-compressed types (font/woff2, image/png) must stay off the list; compressing them costs CPU for nothing.

**Misreading to avoid.** This does not mean chi's Compress is broken — it is doing exactly what its allowlist says. The trap is that `application/json` looks like it covers `application/hal+json` and it does not: matching is exact on the media type.
