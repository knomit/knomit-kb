---
type: observation
domain: [repos, remote, auth, clone, testing]
confidence: 0.9
sources: 1
entities: [Manager.initClone, authConfigFromSpec, Manager.ResolveAuth, config.RemoteAuthConfig, OriginSpec.AuthMethod, OriginSpec.AuthToken, Deps.KeyPath, configureCrypt, store.NewCrypt, remoteIndex.SetRemote, Storyboard.Connect, RepoHandle.WithRemoteAuth, makeRemoteAuthFn]
refs: ['src://7b4887ce51d9/internal/repos/lifecycle.go@0df3b891b86627bcce901c448974ad9cc99deb10:2edd77e3d3ff69c910e3439472b42c16ba6a940b', 'src://7b4887ce51d9/internal/repos/manager.go@0df3b891b86627bcce901c448974ad9cc99deb10:06cf7eeab4383ed0c642ce38e3b8b887040c2f2a', 'src://7b4887ce51d9/internal/store/remote.go@0df3b891b86627bcce901c448974ad9cc99deb10:dd82c0536f87a3902300d9ae2e92af3e78ab8671', 'src://7b4887ce51d9/internal/repos/builder.go@0df3b891b86627bcce901c448974ad9cc99deb10:a10b69cbf5d20057fbef3be5fafc42bade0fe9c0', 'kb://3ec012f5b4d2/kb/conventions/repos/remote/basic-auth-credentials/040af9c3.md', 'kb://3ec012f5b4d2/kb/invariants/repos/clone-persists-resolved-upstream/ca2246c6.md', 'kb://3ec012f5b4d2/kb/gotchas/repos/sync/activatesync-honours-disable-flag/748f72dd.md']
---
# A clone's credential comes from the CreateSpec's OriginSpec, NOT cfg.Remote — and persisting it requires Deps.KeyPath

Manager.initClone resolves the clone credential from `authConfigFromSpec(spec.Origin)` — the AuthMethod/AuthToken carried on the CreateSpec's OriginSpec. The server-level `cfg.Remote` (config.RemoteAuthConfig) is NOT consulted for the credential: Manager.ResolveAuth copies exactly ONE field off it, `KnownHosts`, when the spec leaves it empty, and nothing else. A caller that sets cfg.Remote.User/Password/Token and then calls Create in clone mode clones ANONYMOUSLY, and an authenticating remote answers 401 ("authentication required: unauthorized") from InitFromRemote's fetch.

Consequence, and they are a pair — supplying the credential in the spec is only half of it: initClone finishes by calling `svc.Remote().SetRemote(..., spec.Origin.AuthMethod, spec.Origin.AuthToken)`, and SetRemote REFUSES a non-empty authToken when the store has no Crypt ("refusing to store credential ...: encryption unavailable"), returning an error that fails the whole create at persist-origin. The Crypt is built by configureCrypt from `Deps.KeyPath`; an unreadable/empty KeyPath only WARNS at that point, so the failure lands later, at SetRemote, looking unrelated. So any caller doing a credentialled clone-mode Create must boot its Manager with a readable Deps.KeyPath. Key material is arbitrary bytes — store.NewCrypt runs HKDF over whatever the file contains — so a test harness can use any non-empty file.

Basic auth packs the pair into the single token field as "user:password", split on the first colon by authConfigFromSpec.

WHAT THIS DOES NOT MEAN: cfg.Remote is not dead. It still feeds the RECONCILE path (makeRemoteAuthFn / runReconcileLoop) as the fallback under the persisted remotes row, and its KnownHosts is inherited by spec-driven SSH clones. The split is initial-clone-reads-the-spec, reconcile-reads-the-record-over-cfg.

How this surfaced: removing the special "core" repo (PR #73) made Manager.Create the only way a repo is born, so the Storyboard test harness's Connect() switched from re-booting with cfg.Git.Origin (which DID resolve cfg.Remote, via the deleted initDefaultGit) to Manager.Create in clone mode. WithRemoteAuth kept setting cfg.Remote, the credential stopped reaching the clone, and the token-expiry contract cell failed its precondition against a basic-auth remote. Fixed by mapping cfg.Remote onto the OriginSpec and booting that manager with a sandbox key file.
