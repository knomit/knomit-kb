---
type: observation
domain: [repos, lifecycle, web]
confidence: 0.9
sources: 0
entities: [Manager.Create, Manager.StartCreate, CreateJob, Manager.CreateJobByID, Manager.Archive, Manager.ListArchived, Manager.Restore, Manager.Purge, CreateSpec, ArchiveInfo, Manager.ActiveRepoWithOrigin, reserveNameAndOrigin, ErrCreateInFlight, ErrOriginInUse, RecordRepoID, repos_active_repo_id, RepoInstance.shutdown, internal/repos/lifecycle.go, internal/repos/create_job.go]
refs: ['kb://3ec012f5b4d2/kb/architecture/repos/package-extraction/33c06f14.md', 'kb://3ec012f5b4d2/kb/architecture/repos/builder-step-chain/2ad96627.md', 'kb://3ec012f5b4d2/kb/invariants/repos/one-local-copy-per-knowledge-base/b27a57c6.md', 'kb://3ec012f5b4d2/kb/decisions/repos/lifecycle/5a08fcd2.md', 'src://7b4887ce51d9/internal/repos/lifecycle.go@4baae4ed3043cbcb37a797b625f5e8a1a071f912:1d23011f07edfc6ae9fea777ccbf4297df366f2e', 'src://7b4887ce51d9/internal/repos/registry.go@4baae4ed3043cbcb37a797b625f5e8a1a071f912:865b265d9634c6cb18245a9d9b1c10790752439a', 'src://7b4887ce51d9/internal/repos/origins.go@4baae4ed3043cbcb37a797b625f5e8a1a071f912:914b1d8df520925fb423a601f04820c9792db071', 'src://7b4887ce51d9/internal/repos/create_job.go@7374e81198e2d369b1843f42b7820f2a1ab5288b:3051a39f4ae7d4a8c8f31d9443f907b5fbd7be0f', 'https://github.com/knomit/knomit/pull/162']
---
# Manager runtime lifecycle: Create/Archive/ListArchived/Restore/Purge live in internal/repos/lifecycle.go; Archive/Restore/Purge are registry state flips — the .db file never moves

internal/repos/lifecycle.go adds full runtime repo lifecycle on Manager (no process restart, no CLI):

- Create(ctx, CreateSpec, emit func(Event)): one entry for three modes — preset, custom (ontology YAML bytes), clone (svc via store.Open + InitFromRemote, then persist the origin via m.origins.Set(uid, Origin{...}) into control.db's repo_origins, + ActivateSync). It mints a fresh uid (ksuid.New()) and INSERTs the registry row FIRST (m.reg.Insert), so the file path `m.RepoPath(uid)` = `<home>/repos/<uid>.db` is fresh by construction — there is no 'leftover .db file' collision to guard against, because a name can no longer collide with a file (the name never appears in the path). It then seeds the .db on disk and calls the existing Add→openOne step chain to register. An in-flight set (m.creating, guarded by m.inflightMu, via reserveNameAndOrigin) rejects concurrent same-name creates with ErrCreateInFlight, and same-origin clones with ErrOriginInUse. On any failure the partial .db (+wal/shm) and the registry row are cleaned up (rollback). After Add succeeds, Create calls `m.reg.RecordRepoID(uid, ri.ID())` to enforce the one-local-copy-per-knowledge-base uniqueness constraint (repos_active_repo_id) — an ErrRepoAlreadyRegistered here rolls the whole create back.

- **UPDATE (#67 / PR #162, 2026-08-26): Create is no longer invoked directly by the HTTP layer — `Manager.StartCreate(spec) *CreateJob` (internal/repos/create_job.go) is.** StartCreate runs Create on a goroutine with `context.WithTimeout(m.ctx, m.createTimeout())` — parented to the MANAGER, never to a request — and returns a CreateJob to observe (Status/Result/Done); `Manager.CreateJobByID` looks one up for the poll endpoint, and finished jobs are reaped past CreateJobTTL. StartCreate deliberately takes NO ctx parameter: one there is an invitation to pass the request's, which was the bug. Create itself keeps its signature and remains the synchronous worker.

  This sentence previously read that CreatePreflight's cheap checks "must surface as an HTTP status BEFORE the NDJSON stream starts". **There is no NDJSON stream anymore.** CreatePreflight still runs the cheap checks (name validity, name-active, in-flight, clone origin-uniqueness) and they must still surface as an HTTP status — now BEFORE the 202 Accepted is committed, because once the job has started its failures are poll results rather than statuses. Preflight runs on the REQUEST's context, correctly; the create does not. See kb/decisions/repos/lifecycle/5a08fcd2.md for the wire contract.

  Load-bearing consequence for anyone editing this area: the create's bounded context must NEVER become an ancestor of the builder's indexCtx. Add→openOne takes no context and reads m.ctx, which is what keeps them siblings; threading the create context down would reinstate kb/incidents/repos/clone-create-index-stuck-indexing.

- **UPDATE (repo-registry-in-control.db, 2026-08): Archive/Restore/Purge no longer move the database file.** Archive(name): the not-found check, lens-reference check (`m.registry.RefsRepo(uid)` — keyed by registry UID, never name), and the state flip happen atomically under m.mu.Lock(): `m.reg.SetState(uid, StateArchived, now)` is a single control.db UPDATE, then the repo is removed from the live maps (m.repos/m.byUID/m.unavailable). ANY repo may be archived, including the last one — there is no default/trunk repo to protect (the old ErrCannotArchiveLast/ErrCannotArchiveDefault guards and the isDefault concept are gone with the default-repo removal). ri.shutdown() releases the SQLite handle; the ephemeral `.sessions.db` sidecar is deleted. **The .db file itself is never renamed or moved** — there is no `repos/archive/` directory and no `<name>.<unix>.json` manifest anymore; the repo simply stops appearing in `registry.List(StateActive)`. ArchiveInfo.ID is the repo's uid (the same identity Create minted and the key Restore/Purge take), and ArchiveInfo carries the on-disk SizeBytes (stat'd after shutdown, once the WAL has folded back) precisely because archived repos are no longer visible under an obvious filename to `ls`.

- ListArchived() reads `m.reg.List(StateArchived)` (newest archived_at first, then uid as a total-order tiebreak) instead of parsing manifest files. Restore(uid, newName) is a state flip back to active (`m.reg.SetState(uid, StateActive, 0)`) plus an optional `m.reg.Rename(uid, newName)`, then Add — the database path (`m.RepoPath(uid)`) never changes, so restore is TWO UPDATEs, not a file move; a failure after the state flip rolls both UPDATEs back so the file is always left untouched. Purge(uid) checks RefsRepo, then deletes the registry row (`m.reg.Delete`, cascading to repo_origins via ON DELETE CASCADE) BEFORE removing the .db file — row first, file second, deliberately: a failed unlink leaves a harmless orphan file (logged, cleaned by hand), whereas the reverse order would leave a registry row pointing at nothing, which presents as a MISSING repo offering to rehydrate itself. ActiveRepoWithOrigin (backed by m.origins.ActiveRepoWithURL, a JOIN over repos+repo_origins) enforces origin-uniqueness as a cheap preflight on both clone-create and restore; the authoritative identity-uniqueness guard is repos_active_repo_id via RecordRepoID (see kb/invariants/repos/one-local-copy-per-knowledge-base).

Manager.Rescan and the repos/*.db glob-discovery loop are DELETED (see the retracted rescan-discovery-only fact); Start now reads the registry directly (see manager-start-close-lifecycle) and this lifecycle.go is the only in-process create/archive/restore/purge path.
