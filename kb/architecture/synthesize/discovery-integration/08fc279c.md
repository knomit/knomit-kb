---
type: observation
domain: [synthesize, store, config, mcp]
confidence: 0.9
sources: 1
entities: [DiscoverWorkPayload, pipeline_sessions, DiscoveryConfig, RenderDiscoverWorkItem, CreatePipelineSessionWithEffort]
refs: ['kb://3ec012f5b4d2/kb/architecture/store/pipeline-session-phase-cas/30657f2b.md', 'kb://3ec012f5b4d2/kb/architecture/synthesize/raptor-via-work-queue/38693e69.md', 'src://7b4887ce51d9/internal/synthesize/discovery.go@4baae4ed3043cbcb37a797b625f5e8a1a071f912:1f3779204a76db7272423d4c126d88acd8924687', 'src://7b4887ce51d9/internal/store/pipeline_index.go@4baae4ed3043cbcb37a797b625f5e8a1a071f912:d5f8130fac0ea8fbc091e65c1072d189051fea90', 'src://7b4887ce51d9/internal/synthesize/effort.go@4baae4ed3043cbcb37a797b625f5e8a1a071f912:df58d718b72b33d5b326eef562673a526817ca52', 'src://7b4887ce51d9/internal/config/config.go@4baae4ed3043cbcb37a797b625f5e8a1a071f912:a9a2b21791d3abc28d2044a3b5ac70175158cdb7', 'src://7b4887ce51d9/internal/store/pipeline_index.go@285bc7016c84c8aa3c9024b10bbd8e68b65df282:d3495d960ffc896308e04f0f317d12a45035a7ff', 'kb://3ec012f5b4d2/kb/invariants/store/pipeline-sessions/created-by-is-not-identity/6117e0b8.md']
---
# Discovery rides existing pipeline infra: a 'discover' work-item step + a [discovery] config section (effort is in-memory on the Reviewer, NOT a persisted session column)

Discovery reuses the work-stealing pipeline rather than new infra. A new `discover` StepType carries a DiscoverWorkPayload{Direction, Bridge, ScopeLabel} as FactsJSON; review.StartSession (forward) and hypothesize enqueueBackwardBridgeItems (backward) insert them, the dispatchers render the strict default-skip prompt (RenderDiscoverWorkItem), and ContinueSession applies the response through the gate chain. Discover items are priced BELOW the standard prune/distill/hypothesize items so they run last: forward uses forwardDiscoverPriority (base -10 minus rank), backward uses Priority = -100 - i over the blast-ranked list — both always negative, can't leapfrog a positive standard item, and both stay above reflectPriority (-100).

WHAT THIS DOES NOT MEAN: there is NO pipeline_sessions.effort column and NO CreatePipelineSessionWithEffort. An earlier revision added them; commit 6fb6a79 ('drop dead session effort persistence') removed both. Effort is now carried in-memory on the Reviewer (r.effort, internal/synthesize/effort.go), resolved from config/args at session start — never read back from a session row. CreatePipelineSession is the only constructor and inserts no effort. Do NOT query or write pipeline_sessions.effort — it does not exist.

SIGNATURE NOTE (refreshed after knomit#123). The constructor is now `CreatePipelineSession(ctx, tool, branch, createdBy)`; earlier revisions of this fact quoted the 3-argument form `(ctx, tool, branch)`. The added argument is the session's correlation handle for `pipeline_sessions.created_by`, and it does NOT weaken the claim above — there is still no effort argument and no effort column. Read the quoted signature as evidence about EFFORT, not as a current parameter list; the parameter list is what the source says. (The `CreatePipelineSessionWithEffort` entry in this fact's entity list is deliberate: it is there as a NAMED MISREADING, a symbol that does not exist, not as a stale reference to one that does.)

Thresholds come from a [discovery] config section (effort_default, confidence_threshold, blast_radius_threshold, bridge) + env overrides, surfaced through RepoInstance accessors (DiscoveryEffortDefault/DiscoveryConfidenceThreshold/DiscoveryBlastRadiusThreshold/DiscoveryBridge, plus later coherence/quality/weight accessors). Genuine per-repo overrides remain a follow-on — these are global Config fields surfaced through RepoInstance, the existing idiom.
