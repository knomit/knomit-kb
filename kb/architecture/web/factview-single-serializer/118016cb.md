---
type: principle
domain: [web, hal, fact]
confidence: 0.95
sources: 0
entities: [FactView, BuildFactView, buildFactLinks, factParentTopic, AsOf]
refs: ['src://knomit/internal/web/fact_view.go@307b67d']
---
# One FactView struct + BuildFactView serializes both HEAD and commit-anchored fact responses

ONE FactView struct + ONE BuildFactView function serializes BOTH HEAD-anchored and commit-anchored fact responses. The link set differs by anchor: HEAD view has {self, incoming, outgoing, commits, snapshot, parent, branch}; commit-anchored has {self, incoming, outgoing, commits, live, commit, parent, branch}. 'snapshot' (HEAD only) is the immutable citation pin at the current head sha — the only way to get a stable URL out of a HEAD view. 'live' (commit-anchored only) is the URL back to HEAD. 'commit' (commit-anchored only) points at the parent commit resource. Both shapes carry incoming + outgoing (commit-anchored incoming returns version-aware lineage — see store/derived-from-time-aware-edges). factParentTopic derives the parent topic URL by stripping the ontology root prefix (first path segment) and the filename (last segment) from the fact's path. FactView.MarshalJSON is overridden so the Links field emits under the canonical '_links' key.
