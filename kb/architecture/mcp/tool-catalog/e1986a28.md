---
type: reference
domain: [architecture, mcp, documentation]
confidence: 0.9
sources: 1
entities: [knomit_learn, knomit_query, knomit_explain, knomit_update, knomit_retract, knomit_review, knomit_hypothesize, knomit_repos, knomit_bind, enabledTools, toolRegistrations, mcpDispatch, SessionBindingMiddleware, gateBinding, binding_handles]
refs: ['kb://3ec012f5b4d2/kb/decisions/mcp/profile-per-repo/f192649f.md', 'kb://3ec012f5b4d2/kb/decisions/mcp/session-binding/unscoped-mount/a1019a29.md', 'kb://3ec012f5b4d2/kb/decisions/mcp/session-binding/catalog-tool/314b18e6.md', 'kb://3ec012f5b4d2/kb/decisions/mcp/session-binding/per-bind-handle/5f22038d.md', 'kb://3ec012f5b4d2/kb/invariants/mcp/session-binding/handle-is-the-only-router/5dd2cf17.md', 'kb://3ec012f5b4d2/kb/invariants/mcp/binding-gate/registration-seam/8cdea548.md', 'src://7b4887ce51d9/internal/mcp/server.go@b3cebeb4d6255ec5e9c1e58498dae1c106e64932:ffb6e5142444827a715a5fd5c5a40c0632aebfae', 'src://7b4887ce51d9/internal/mcp/repos.go@b3cebeb4d6255ec5e9c1e58498dae1c106e64932:1ac64bb99dd060caf96a0f37999513234636a60b', 'src://7b4887ce51d9/internal/mcp/bind.go@b3cebeb4d6255ec5e9c1e58498dae1c106e64932:7aa4344c13bdda06c743399405b045e263367cde', 'src://7b4887ce51d9/internal/mcp/binding_gate.go@b3cebeb4d6255ec5e9c1e58498dae1c106e64932:efb55bbb42021c4b07fd3e399069a07f6672c91f', 'src://7b4887ce51d9/internal/web/router.go@2eed2b999bc8f153786e542078a973bc85ee4f15:c4cac3f695a3a3e560d56430f29c885484cf2e29', 'src://7b4887ce51d9/internal/mcp/instructions.go@2eed2b999bc8f153786e542078a973bc85ee4f15:56cb24424bd98abbea46173481258f120ed51355', 'src://7b4887ce51d9/internal/mcp/update.go@2eed2b999bc8f153786e542078a973bc85ee4f15:4b26617b61752affc34f31c2b7e7a97fb34cd22a', 'src://7b4887ce51d9/internal/web/server.go@2eed2b999bc8f153786e542078a973bc85ee4f15:0feb5c66211371f42e4564c4be326711743b883a']
---
# knomit registers 9 MCP tools (4 in read-only mode) on ONE shared server; mounted per-branch, per-lens, and unscoped — where the target is named per CALL by a `binding` handle, not by the session

The MCP surface is NINE tools, listed in `toolRegistrations` (internal/mcp/server.go) in registration order with their write flags:

| tool | write? |
|---|---|
| knomit_learn (write facts in one commit) | yes |
| knomit_query (paginated snippet search) | no |
| knomit_explain (walk versioned provenance) | no |
| knomit_update (revise in place) | yes |
| knomit_retract (commit a removal) | yes |
| knomit_hypothesize (generate hypothesis facts; user-initiated) | yes |
| knomit_review (work-stealing prune/distill/reflect) | yes |
| knomit_repos (THE discovery tool: every repo and lens on the server, plus what a given handle points at) | no |
| knomit_bind (mint a handle naming a repo or lens) | no |

knomit_bind arrived with the unscoped mount (PR #209). A tenth tool, knomit_catalog, was built and then FOLDED INTO knomit_repos before it shipped (PR #210) — so a reader who finds knomit_catalog referenced anywhere is looking at something written mid-flight; it never existed on dev.

**THE `binding` ARGUMENT (PR #215, merged b3cebeb4).** On the unscoped mount every tool EXCEPT knomit_repos and knomit_bind takes a REQUIRED `binding` argument: the opaque handle knomit_bind returns. knomit_repos takes it optionally; knomit_bind takes none, since it is where handles come from. On a URL-scoped mount the argument is refused. Nothing is keyed on Mcp-Session-Id — see kb/invariants/mcp/session-binding/handle-is-the-only-router. The gate that enforces it wraps each handler AT REGISTRATION rather than as middleware, for reasons in kb/invariants/mcp/binding-gate/registration-seam.

knomit_repos needs NO BINDING and returns `{repos, lenses | lenses_error, bound?}`: every repo on the server; every lens (the `lenses` key is always present on success, `[]` when the server has none); and — only when the call carried a handle that resolved — a `bound` section carrying the target's NAME and mount table, or `status: "unresolvable"` with the reason when the handle's pin no longer resolves. NOTE the key: `bound.name` is the repo or lens name; the key `binding` means the opaque HANDLE everywhere on this surface and never a name (it was `bound.binding` before #215). When the lens registry lookup FAILS, the `lenses` key is ABSENT and `lenses_error` carries the text while `repos` and `bound` are still returned, so a caller never loses its own mount table over an unrelated control-plane error and can never read a failure as "no lens exists" (the field is a `*[]T` with omitempty for exactly that reason: a plain slice would marshal zero lenses and nil identically). The shape is fixed PER STATE, not per session: each key's presence reports a fact about the world, never a mode the tool is in.

knomit_bind returns `{binding, name, mounts}` — the handle FIRST — plus the bound base's instructions; never the catalogue. It and knomit_repos build the mount table through one shared function so they cannot drift.

`knomit_update` list fields (domain, entities, refs) replace WHOLESALE: send the complete new list; omit the field to leave it unchanged (internal/mcp/update.go: `updates.Refs != nil` → `fact.Refs = refs`).

**readOnly GATES the tool set.** `NewServer(defaultOntologyRoot, mgr, readOnly, embedders...)` runs `enabledTools(toolRegistrations(mgr, embedders...), readOnly)`: when readOnly=true every write tool is dropped and only the FOUR read tools register — knomit_query, knomit_explain, knomit_repos, knomit_bind. So a read-only ('demo') instance exposes 4 tools, not 9. knomit_repos and knomit_bind are write=false deliberately: without them nothing on the unscoped mount could be discovered, bound, and therefore read.

**MOUNT POINTS — there are THREE**, all dispatching to the same handler (`mcpDispatch`, internal/web/router.go), each accepting `/mcp` and `/mcp/*`:
- repo+branch-scoped: `/api/v1/repos/{repo}/branches/{branch}/mcp`
- lens-scoped: `/api/v1/lenses/{lens}/mcp`
- unscoped: `/api/v1/mcp` — no repo or lens in the URL; a caller mints a handle with knomit_bind and names it on every subsequent call. Formerly called the 'session-bound' mount, which is now a misnomer: several independent callers may share one connection and hold different handles at the same time.

WHAT THIS DOES NOT MEAN:
- **Profile does NOT key servers and does NOT gate tools.** There is exactly ONE MCP server instance (`s.mcpHandler`), not three. Profile is a per-repo setting in control.db that changes ONLY the per-session instruction addendum; it never adds or removes a tool — readOnly does. `?profile=` on the endpoint is accepted, logged at debug, and IGNORED. See kb/decisions/mcp/profile-per-repo/f192649f.md.
- '9 tools' is the read-WRITE count. Do not assume a client can call learn/update/retract/review/hypothesize against a read-only instance — those 5 write tools are absent from its catalog entirely.
- **Every tool fails closed without a valid handle EXCEPT knomit_repos.** Seven return a tool error naming knomit_bind; knomit_repos answers, because it is how an agent learns the names knomit_bind accepts, and it REPORTS a bad handle under `bound` rather than raising. Do not 'restore consistency' by making it fail too.
- The tool list is NOT filtered by the binding's write eligibility. A caller whose handle names a subscription (a read-only follower) still SEES the write tools listed; they fail at call time on Binding.WriteOK(). tools/list is per server, not per session.
- The lens mount is not a second server; a lens authors under its WRITE repo's profile.
- The unscoped mount is not an authorization boundary. Any repo is already reachable by URL, so a handle grants nothing new; the network remains the boundary. The handle is unguessable so that a model cannot conjure a valid one from a name, not to keep anyone out.

Clients usually reach this via knomit-bridge (stdio). With neither --repo nor --lens the bridge connects to the unscoped mount and forwards the `binding` argument untouched.
