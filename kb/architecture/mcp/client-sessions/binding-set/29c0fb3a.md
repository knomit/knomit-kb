---
type: reference
domain: [mcp, web, store, sessions, control-db, repos]
confidence: 0.95
sources: 2
evidence_weight: 0.6551724137931034
entities: [client_session_bindings, client_sessions, RecordSessionBinding, Store.SessionBindings, Store.appendBindings, BindingChunkSize, repos.ResolvedBinding, repos.PinRecorder, recordClientSession, handleHALClientSessions, bindingNames, Filter.Binding, Store.Purge, Store.Touch, binding_handles, handle_experiments, NewBindingHandle, MintBindingHandle, Store.BindingHandle, Store.HandleExperiment, Store.SetHandleExperiment, Store.ClearHandleExperiment, BindingPinFromContext, ResolveSessionBinding, ResolveSessionBindingOnExperiment, NewBindingOfRepo, 000005_binding_handles.up.sql, 000007_handle_experiments.up.sql]
motifs: [last-value-hides-set, join-inflates-count, state-keyed-by-capability]
refs: ['kb://3ec012f5b4d2/kb/architecture/mcp/session-binding/handle-model/4bf6c1e1.md', 'kb://3ec012f5b4d2/kb/invariants/mcp/session-binding/handle-is-the-only-router/5dd2cf17.md', 'kb://3ec012f5b4d2/kb/invariants/mcp/client-sessions/98c87890.md', 'kb://3ec012f5b4d2/kb/decisions/mcp/client-sessions/storage-control-db/2a30895a.md', 'src://7b4887ce51d9/internal/store/migrate/control/000006_client_session_bindings.up.sql@17d520bee1790dd8714a83fa1b91b04cda695371:c3d543315f79bbdcbb056b0d7c2ae4ee37aa1395', 'src://7b4887ce51d9/internal/client/sessions/session_bindings.go@caa0d860e8dd28517f1b37666aeeb8bbbf1284bd:3e1bffbeb28b96f9a3810de71a7f92bf2bf9c145', 'src://7b4887ce51d9/internal/client/sessions/store.go@caa0d860e8dd28517f1b37666aeeb8bbbf1284bd:ca3ec5c7ea1e10a4ed05ec033787b8779095e515', 'src://7b4887ce51d9/internal/web/client_sessions.go@17d520bee1790dd8714a83fa1b91b04cda695371:a63fe1e5b74441441cc044c4da89c7fc4a79fd12', 'src://7b4887ce51d9/internal/repos/context.go@17d520bee1790dd8714a83fa1b91b04cda695371:846ee00070e3449237da4d4089c0a0362fa261e8', 'https://github.com/knomit/knomit/pull/216', 'kb://3ec012f5b4d2/kb/incidents/mcp/session-binding/cross-binding-overwrite/17935f4f.md', 'kb://3ec012f5b4d2/kb/architecture/store/migrations/ec215752.md', 'kb://3ec012f5b4d2/kb/decisions/store/migrations/control-db-golang-migrate/c06bcaae.md', 'kb://3ec012f5b4d2/kb/decisions/mcp/experiments/handle-keyed-sibling-table/d0f37357.md', 'kb://3ec012f5b4d2/kb/invariants/mcp/experiments/one-answer-per-handle/09a98db6.md', 'src://7b4887ce51d9/internal/store/migrate/control/000005_binding_handles.up.sql@b3cebeb4d6255ec5e9c1e58498dae1c106e64932:abbdd6332e8f5f991ffedc20184fd29549a28e13', 'src://7b4887ce51d9/internal/client/sessions/bindings.go@b3cebeb4d6255ec5e9c1e58498dae1c106e64932:e267c7548efb4d13183432d2739b3a45dfbd9198', 'src://7b4887ce51d9/internal/client/sessions/store.go@b3cebeb4d6255ec5e9c1e58498dae1c106e64932:787ffb9e8eb0f3fc5839827719009df64055667e', 'src://7b4887ce51d9/internal/repos/context.go@b3cebeb4d6255ec5e9c1e58498dae1c106e64932:592fb0a6a27215c9ca495323752e858e3f5581b1', 'src://7b4887ce51d9/internal/repos/session_binding.go@b3cebeb4d6255ec5e9c1e58498dae1c106e64932:4e2f389a79d837d824713f786bc3502a254ead69', 'src://7b4887ce51d9/internal/store/migrate/control/000007_handle_experiments.up.sql@55cd390dcdfbad97f9d5f6774cfab66ff783bd2e:981409ade3a51571c726289ca626cdc8b88bc62e', 'src://7b4887ce51d9/internal/client/sessions/bindings.go@55cd390dcdfbad97f9d5f6774cfab66ff783bd2e:2c1277f0f9fc25a881e53ef23690ca8a282c7ff4', 'src://7b4887ce51d9/internal/repos/experiment_binding.go@55cd390dcdfbad97f9d5f6774cfab66ff783bd2e:278bfabbcfb63d60431ecb87380063580b5ec33a']
---
# client_session_bindings is the SET of handles one MCP session has presented, keyed by (session_id, handle) so two handles on one repo are two rows — client_sessions.binding stays as the LAST-seen pin, and ?binding= matches any row via EXISTS and returns the session once

SCHEMA (control.db, migration `000006_client_session_bindings.up.sql`):
`client_session_bindings(session_id, handle, binding, branch, first_seen_at, last_seen_at, request_count)`, PRIMARY KEY `(session_id, handle)`, plus an index on `binding`.

WHY IT EXISTS. Since binding became a per-call handle, one `Mcp-Session-Id` can serve several independent callers at once. `client_sessions.binding` holds the LAST pin seen, so it stopped describing a session and became a snapshot of whichever call landed most recently. This table is the set.

KEYED BY HANDLE, NOT BY PIN. Two handles naming the same repo are TWO ROWS, because they are two callers; collapsing them by pin would discard exactly the distinction the handle exists to make. The PK is what enforces it.

HOW IT IS STAMPED. `recordClientSession` reads `repos.ResolvedBindingFromContext` ONCE (handle, pin, branch) and uses it twice: the pin goes to `Store.Touch` for `client_sessions.binding`, and the whole triple goes to `Store.RecordSessionBinding` — an upsert that inserts at count 1 and afterwards bumps `last_seen_at` and the count while leaving `first_seen_at` alone. Reading the context twice could DISAGREE, because a task-augmented tool call may still be writing into the recorder while this runs; that is also why `PinRecorder` is mutex-guarded.

ONLY A REQUEST THAT RESOLVED A HANDLE ADDS A ROW. A URL-scoped request has a pin but no handle, so it stamps `client_sessions.binding` as before and contributes NOTHING here. That asymmetry is correct rather than an omission: the set answers "which handles has this session presented", and a URL-scoped caller presented none. Such a session's `bindings` array is empty, and the API serialises it as `[]`, never `null`.

READING IT. `Store.SessionBindings(ctx, sids)` returns every row for a page of sessions, most-recently-used first within each session (`ORDER BY session_id, last_seen_at DESC, handle`).

It is ONE QUERY PER CHUNK of `BindingChunkSize` (900) session ids — NOT one query overall, and not one per session. The property that matters is per-chunk rather than per-ROW: control.db runs at `SetMaxOpenConns(1)`, so a lookup per session row would serialise the whole response behind a single connection, which is the same reason the handler builds one `bindingNames` index instead of resolving each row. `Store.List`'s limit bounds the number of chunks, and at the default page size of 500 there is in fact exactly one query — which is why the earlier "ONE query" wording read as true and why nobody should now "fix" the chunking back out. 900 is deliberately conservative and deliberately NOT measured: `SQLITE_MAX_VARIABLE_NUMBER` is a COMPILE-TIME limit, not a property of SQLite (32766 on the bundled 3.51.2, but 999 before 3.32, and a differently-built system library can still be 999 today), so a chunk size derived from measuring the current library is a constant that breaks silently on the one machine that differs. It is exported so a test can seed past the REAL boundary rather than exercise a test-only small chunk. An empty `sids` is not a query at all, and the last partial chunk is included — dropping it returns no error and reads downstream as "presented no handles", indistinguishable from the truth.

A failure DEGRADES — the handler logs, drops the sets and still serves the rows, because the last-seen pin is on the row itself and losing the page over the richer view would be the worse trade.

FILTERING. `?binding=` matches a session if ANY binding it has presented is that pin: `binding = ? OR EXISTS (SELECT 1 FROM client_session_bindings b WHERE b.session_id = client_sessions.id AND b.binding = ?)`. Two halves, both load-bearing. EXISTS rather than a JOIN because a session with three handles on one pin is ONE session — a join would return it three times and corrupt every count built on the list. And the legacy `binding = ?` half is what keeps a session recorded only on the old column matching, so the filter does not regress for rows written before this table existed.

LIFETIME. The set belongs to its session and dies with it: `Store.Purge` deletes rows whose `session_id` is no longer in `client_sessions`. That is deliberately UNLIKE `binding_handles`, which is routing state a live caller may still present and therefore ages on its own `last_used_at`.

WHAT THIS DOES NOT MEAN. These rows are OBSERVATIONAL, exactly like every column of `client_sessions` and for the same reason — inferred from request traffic, never to be gated on. Routing was decided by the handle argument on the call itself. A row here says only "this session was seen presenting this handle". It is also NOT the routing table: that is `binding_handles`, which maps a handle to its target and is what the gate reads.
