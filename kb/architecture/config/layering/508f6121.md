---
type: reference
domain: [architecture, config, documentation]
confidence: 0.9
sources: 1
entities: [config.Load, config.Defaults, KNOMIT_HOME, config.toml]
refs: ['src://7b4887ce51d9/internal/config/config.go@8618fc03bade0641a79bbd57365eabc66158c8f1:9f2eff10dc383256b0778eef64696e75462cdadb', 'src://7b4887ce51d9/internal/config/paths.go@8618fc03bade0641a79bbd57365eabc66158c8f1:353d7e68820aa1a589cc48bac4500a404dd51419', 'kb://3ec012f5b4d2/kb/gotchas/config/toml-beside-executable/286380c8.md']
---
# Config layers defaults -> knomit.toml -> env; KNOMIT_HOME resolved first

config.Load() layers configuration lowest-to-highest: hardcoded Defaults() -> knomit.toml -> environment variables (each env var overlays only if set).

KNOMIT_HOME (KNOMIT_REPO is a backward-compat alias; the default is per-OS, see config.DefaultHome) is resolved FIRST, in config.homeAndConfig, because it is the config search root: tilde-expanded, then made absolute. It therefore cannot be overridden from inside the TOML (Load restores Home after decoding the TOML).

The config file is named knomit.toml (NOT config.toml) and is read ONLY from <home>/knomit.toml (findConfigFile). It is never looked for beside the binary.

In the TOML, home/host/port/socket/ontology_root/onnx_lib_path/local_origin_root/methodology_min_score are BARE top-level keys (the home key is spelled `repo`). The rest are tables: [llm], [embeddings], [git], [remote], [cluster_cache], [session], [discovery].

Key defaults: host=localhost, port=19278, ontology_root=kb, embeddings.model=embeddinggemma, llm.provider=gemini/model=gemini-2.5-flash, git.serve=true.

Malformed numeric/duration env values fail at boot rather than being ignored. Some knobs are env-only (no TOML key): KNOMIT_REFLECT_PROPOSE_CAP, KNOMIT_REFLECT_NOVELTY_THRESHOLD, KNOMIT_PPROF_ADDR, KNOMIT_BASE_URL, KNOMIT_MCP_DEBUG.

WHAT THIS DOES NOT MEAN:
- The config file is NOT `config.toml`: a config.toml in KNOMIT_HOME is silently IGNORED.
- Nor is a knomit.toml placed next to the binary read: only <home>/knomit.toml is (config.go findConfigFile).
