---
type: synthesis
domain: [store, schema, architecture]
confidence: 0.85
sources: 1
origin: distilled
entities: [GraphSchemaVersion, meta.graph_schema_version, Rebuild, fact_domain_tokens, repopulateDomainTokens]
refs: ['src://knomit/internal/store/search_index.go@2a9689388fb4025e7f169e19031a02b6292e1750', 'src://knomit/internal/store/migrate/repo/000008_fact_domain_tokens.up.sql@2a9689388fb4025e7f169e19031a02b6292e1750', 'src://knomit/internal/store/domain_match.go@2a9689388fb4025e7f169e19031a02b6292e1750', 'src://7b4887ce51d9/internal/store/migrate/repo/000018_abstraction_axis.up.sql@b57785357448cbbca9b3b43607fe33987f5522f2:91f2d8db2ac8f8c4fe810ec006da0485fa62bcc2', 'src://7b4887ce51d9/internal/store/vec_table.go@b57785357448cbbca9b3b43607fe33987f5522f2:b25e78fc364a3e62dfb0ebf60fdc91c8bc4199af', 'src://7b4887ce51d9/internal/store/search_index.go@b57785357448cbbca9b3b43607fe33987f5522f2:e07ca09c5a7b7ae271178805c0aec3e6bdafd2d6', 'kb://3ec012f5b4d2/kb/architecture/store/fast-rebuild-three-phase/0a1400eb.md', 'kb://3ec012f5b4d2/kb/architecture/store/graph-schema-version/028172a8.md', 'kb://3ec012f5b4d2/kb/architecture/store/domain-token-containment-matching/70af15fb.md', 'kb://3ec012f5b4d2/kb/architecture/store/junction-tables-for-filtering/105b3ce3.md', 'kb://3ec012f5b4d2/kb/architecture/store/abstraction-axis/75a47ad3.md']
---
# Derived SQLite index state is regenerated from git, never data-migrated — new derived shapes ship an empty-table migration + a GraphSchemaVersion bump

CROSS-CUTTING PATTERN: every piece of SQLite-side state DERIVED from the git fact tree — facts rows, facts_vec embeddings, the cypher graph, fact_domains/fact_entities junctions, and fact_domain_tokens (PR #70) — is treated as rebuildable cache, never as a thing to migrate data into. Git is the only source of truth.

THE RECIPE for changing the shape of derived state:
1. Give the empty shape to the schema. USUALLY that is a numbered .up.sql migration creating ONLY the empty table/index — NO data backfill (migration 000008's comment: 'Derived index state: rebuilt from git, no data backfill needed here; next rebuild populates it.'). But NOT ALWAYS: a shape that must vary at runtime is code-managed — facts_vec is created by ensureFactsVec at the active embedding model's dimension, deliberately with NO static FLOAT[768] migration (search_index.go v4 note).
2. Force existing deployments to regenerate it. USUALLY a bump of searchIndex.GraphSchemaVersion (persisted in meta.graph_schema_version). But NOT ALWAYS: the embedding vectors regenerate on EMBEDDING-IDENTITY drift instead — NeedsRebuild ALSO returns true when meta.embed_model_id / meta.embed_dim no longer match the active embedder, with no version bump (search_index.go).
3. The trigger (version mismatch OR embedding-identity mismatch) is detected at startup (NeedsRebuild) and drives a full Rebuild (ON CONFLICT(path,blob_hash) DO UPDATE) that regenerates ALL derived state from the git tree + commit_log. Rowid preservation means embeddings SURVIVE a version-only heal (no re-embed) — but an embedding-identity heal recreates facts_vec empty and DOES re-embed the whole corpus.

WHY safe: because the data is reconstructable from git, a full reindex is ALWAYS a valid no-data-loss operation, so the team never writes/tests a forward data-migration for derived state — schema files carry only shape, and correctness of derived content lives in ONE place (the rebuild path). Contrast: AUTHORED state (facts.domain/entities JSON, raw immutable fact_domains rows) is NOT rebuilt-and-replaced — it round-trips from the markdown; only the CANONICAL/derived projections are regenerated.

A SECOND WORKED EXAMPLE OF BOTH "NOT ALWAYS" CLAUSES — the abstraction axis (migration 000018, motif Phase 0, PR #99):
- **It ships with NO GraphSchemaVersion bump, and that is correct.** Verified: the constant is "5" both before and after the whole motif stack. The bump exists to force regeneration of state a rebuild would otherwise leave STALE — and EMPTY is the correct state here: the review pipeline fills the axis lazily under its own latency budget, and the pair cache is derived from whatever the axis holds. **The test for step 2 is therefore not "is this derived state new?" but "would an empty table be WRONG?"** If empty is correct, a bump would only force a needless full reindex on every deployment.
- **A runtime-width vec0 table must be validated on EVERY path, not behind the embedding-identity short-circuit.** fact_titles_vec is code-managed (ensureFactTitlesVec) for the same reason facts_vec is. The trap: an upgraded DB bootstraps the table at the DEFAULT width, and the embedding-identity check then MATCHES — so the short-circuit skips the very validation that would have caught the wrong width. The identity check answers "is the model the same?", never "is this table the right shape?".

WHAT THIS DOES NOT MEAN:
- A derived-shape change does NOT always ship a numbered .up.sql migration — a runtime-varying shape (facts_vec, fact_titles_vec) is created in code, not a migration file.
- A rebuild is NOT always gated by a GraphSchemaVersion bump — an embedding model-id/dim change forces one through the embedding-identity check with no version change, and a derived table whose empty state is CORRECT needs no bump at all.
- The derived-state set does NOT end at fact_domain_tokens — facts_vec and fact_titles_vec embeddings are derived cache too, and their regeneration path differs (re-embed, not rowid-preserved).
- An embedding-identity MATCH does NOT imply the derived vec0 tables are structurally valid.
