---
type: observation
domain: [web, incidents, library]
confidence: 0.9
sources: 1
entities: [web/src/Library.tsx, loadMoreRef, useLayoutEffect, IntersectionObserver, Library.lens.test.tsx, lensExhausted]
motifs: [stale-closure-behind-ref, commit-mistaken-for-settled]
refs: ['src://7b4887ce51d9/web/src/Library.tsx@985bdfe8045ff9a9553496c58ef304c2b4591948:3540f79efe55572404af55cf124c86470846d066', 'src://7b4887ce51d9/web/src/Library.lens.test.tsx@985bdfe8045ff9a9553496c58ef304c2b4591948:58b517d9b80bbb482eede416677cdcaca95b6cb6#L680-L805', 'https://github.com/knomit/knomit/issues/270', 'https://github.com/knomit/knomit/pull/273', 'kb://3ec012f5b4d2/kb/gotchas/web/testing/library-loadmore-ref-passive-mirror/a965b6f6.md']
---
# Library could append the same page twice (rows 50-89 duplicated) or stop paging after a scope change: a stale loadMore closure ran between a commit and React's passive flush; fixed by mirroring loadMoreRef in useLayoutEffect (PR #273)

SYMPTOM. It first surfaced as a merge-gate test flake (knomit#270): `expected [0, 50, 50] to deeply equal [0, 50]` in Library.lens.test.tsx on the CI Node 22 leg. It is a PRODUCTION defect in web/src/Library.tsx, not only a test race. In a browser, an IntersectionObserver notification delivered between React's commit and its passive-effect task ran the previous loadMore. In the lens union and repo Recent lists that fetched the offset just loaded, and rows 50-89 were appended twice (duplicate React keys). After a lens scope change it held the old scope's exhausted flag, and the new list never paged. The old observer is still connected during that gap, because its disconnect is itself a passive cleanup. Reach is narrow, since the sentinel must cross its threshold inside the window, but real.

CAUSE. loadMoreRef was mirrored in a passive useEffect while the loading refs its guards read were mirrored during render. Details in the gotcha kb/gotchas/web/testing/library-loadmore-ref-passive-mirror.

FIX. `useLayoutEffect(() => { loadMoreRef.current = loadMore; })`: the ref is current by the end of the commit.

REGRESSION ANCHOR. Library.lens.test.tsx, describe 'Library — the sentinel sees the current list at the commit that shows it'. Its three tests fire the sentinel from a MutationObserver at the commit, and they failed 3/3 before the fix: [0,50,50]; [0,0] (50 never requested); [0,50,50] for repo Recent. If any of them fails again, the stale-closure window is back. Do not 'stabilise' it with waits.

NOT DONE, deliberately: a requested-offset guard as defence in depth. Keeping failed fetches retryable would need resets on failure and on every scope reset, in both paths.
